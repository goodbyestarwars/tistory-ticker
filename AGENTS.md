# AGENTS.md

이 저장소에서 작업하는 ChatGPT, Codex 및 기타 코딩 에이전트용 지침이다.

## 시작 절차

1. `CLAUDE.md`에서 프로젝트 핵심 구조와 공통 규칙을 확인한다.
2. 현재 요청에 필요한 문서와 소스코드만 읽는다.
3. 문서와 실제 코드가 다르면 실제 코드를 기준으로 판단하고 관련 문서를 갱신한다.

## 문서 선택

- 인프라·배포 구조: `ARCHITECTURE.md`
- VM REST API: `API_REFERENCE.md`
- UI 기준: `docs/UI_GUIDE.md`
- 주요 작업이력: `docs/WORK_HISTORY.md`
- Claude 작업 절차: `.claude/skills/`, `.claude/rules/`

## 공통 작업 규칙

- 기존 기능을 임의로 삭제하지 않는다.
- 요청 범위를 벗어난 리팩터링을 하지 않는다.
- 수정 전 관련 파일과 호출 관계를 확인한다.
- 비밀정보를 코드, 문서, 로그, 커밋에 기록하지 않는다.
- 미검증 API 필드와 단위를 추측해 확정값처럼 사용하지 않는다.
- 완료 후 변경 파일, 검증 결과, 배포 여부를 요약한다.

## 작업이력 기록

- 중요한 기능·구조·API·데이터베이스·배포 변경은 별도 요청이 없어도 `docs/WORK_HISTORY.md`에 기록한다.
- 날짜는 KST 기준으로 작성한다.
- 목적, 주요 변경, 검증·배포 결과만 간결하게 기록한다.
- 사소한 문구·스타일 조정은 작업이력에 남기지 않고 Git 커밋으로 관리한다.
- 동일 작업의 후속 수정은 기존 항목에 합치거나 바로 아래에 추가한다.
- 민감정보는 절대 기록하지 않는다.
