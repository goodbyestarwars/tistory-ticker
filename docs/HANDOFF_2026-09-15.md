# 인수인계 2026-09-15 (06:40 KST 기준)

## 0. 기본 정보
- 저장소: `goodbyestarwars/tistory-ticker`, 브랜치 `master`
- 운영 API: https://goodbyestar.cloud
  - 응답은 `{success, updatedAt, data}`로 감싸져 있음
- 운영 사이트: https://ghlee.tistory.com
- VM: GCP `instance-20260712-153038` / `us-central1-c` / e2-micro
  - 메모리 1GB, 스왑 2GB
- 로컬 클론: `C:\Users\goodb\Documents\Codex\2026-08-28\wlr\work\tistory-ticker`

## 1. 사용자 상시 지시
- 완료하면 묻지 말고 머지·배포한다. "완료"는 라이브 반영 확인까지다.
- 서버 부담이 크거나 불가능하면 중단하고 보고한다.
- 사용자는 지금 돈을 쓸 수 없다. 비용이 늘 수 있는 변경은 미리 알린다.
- CLAUDE.md를 따른다.
  - 미검증 API 필드 금지
  - KRX 내부 크롤링 경로 신규 금지
  - 중요 변경은 `docs/WORK_HISTORY.md`에 같은 커밋으로 기록

## 2. 배포·검증 방법
- `js/`·`css/`·`style.css`: master 반영 후 GitHub Pages 자동 배포
  - `max-age=600`이라 최대 10분 지연
- `scripts/cloud-vm/`: VM `kiwoom-deploy.timer`가 5분마다 확인
  - 바뀌었으면 `*.py`를 `~/kiwoom-api`로 복사하고 재시작
  - 반영 확인: `/health`의 `data.deployedCommitShort`
- 부하 확인: `/health/load`
  - loadAvg, 메모리, 스레드별 CPU
- 테스트(Windows): `python -m pytest test -q --continue-on-collection-errors`
  - 기준선: 1005 passed, fcntl 수집 오류 6건은 정상
- VM 명령은 사용자가 VM 터미널에서 직접 실행한다.
  - Cloud Shell은 VM이 아니다.
  - VM 안에서 `gcloud compute ssh` 금지

## 3. 이번 세션 머지 내역
- #436/#438/#439: KIS 실시간 WebSocket 허브
  - 40슬롯 관리, 해지(tr_type 2), 옵션 예산
- #437/#441: "오늘 돈이 몰린 섹터"를 키움 테마로 교체
  - ka90001/ka90002, `/theme-flow`
- #440/#441: 실시간/지연 배지, 정규장·애프터마켓·NXT 표기, REST 보조 조회
- #442/#443: 스캔을 20:10~21:00 KST로 이동, flock으로 한 번에 하나씩 실행
  - #442가 과부하를 일으켜 #443으로 수정
- #444: 장외 시간 수집 주기 완화 (`market_clock.py`)
- #445: `/health/load` 추가, `/health`에 배포 커밋 표시
- #446~#448: 바이낸스 삼성·하이닉스 참고 시세
  - VM은 451로 차단돼 방문자 브라우저가 직접 조회
- #449: 차트검색 캐시 테스트 Windows 간헐 실패 수정
- #450/#451: 메인 "국내 시장" 카드 VI 배지
  - `/api/circuit-breaker`, KIS FHPST01390000을 20초마다 조회
- #452: 거래량 돌파(10분) 스캔이 9/4 이후 매일 1초 만에 죽던 문제 수정
  - 원인: `.env` 미로드 + 키움 `get_token()` 인자 누락 (VM journalctl로 확인)

## 4. 오늘 확인할 것 (시간순)
1. **08:55~ 장중**: VI 데이터 확인
   - `/api/circuit-breaker`의 `data.vi_list`에 실제 VI 종목이 들어오는가
   - 메인 카드에 "VI N건" 배지가 뜨는가
2. **09:10 이후**: 거래량 돌파 첫 정상 실행 확인
   - `/pattern-scan`의 `data.patterns.volumeBreakout` 건수
   - `data.volumeBreakoutScannedAt` 값
   - 실패 시 VM에서 `sudo journalctl -u kiwoom-volumebreakout --since today --no-pager | tail -40`
   - 장외 시간에 수동 실행 금지. 전일 데이터로 가짜 결과가 저장된다.
3. **장중 아무 때**: KIND RSS에 거래소 시장조치 공시가 섞여 오는지 (5장 참고)
4. **20:10~21:00**: 스캔 순차 실행 중 `/health/load` 부하·메모리 확인

## 5. 다음 작업 후보: 사이드카·서킷브레이커 배지
**현재 상태**
- VI 배지만 적용됐다.
- 사이드카는 `sidecar.available=false`로 숨겨져 있다.
- 서킷브레이커는 스키마에 아직 없다.

**찾은 출처: KRX KIND 공시**
- 사이드카 공시 제목 (실제 공시 확인):
  - `[유가증권시장] 유가증권시장 매수 사이드카(Side car) 발동`
  - `유가증권시장 매도 사이드카(Side car) 발동`
- 서킷브레이커 공시 제목:
  - `[유가증권시장] 유가증권시장 매매거래 일시중단(1단계 CB 발동)`
- 올해 사이드카가 79회 발동해 자주 쓰일 기능이다 (언론 보도).

**규칙상 가능한 이유**
- 서버가 이미 KIND 오늘의공시 RSS를 쓰고 있다.
  - `scripts/cloud-vm/domestic_news.py`의 `KIND_RSS_URL`, `_kind_items()`, 30초 캐시
- 새 KRX 경로가 아니다. 금지 대상은 2026-07-11 차단된 KRX 내부 데이터 경로다.

**미확인 사항 (구현 전에 확인할 것)**
- 이 RSS에 거래소 발 시장조치 공시가 포함되는가
  - 06:00엔 RSS가 0건이라 확인 못 함
  - 장중에 `[유가증권시장]`·`[코스닥시장]`으로 시작하는 항목이 오는지 보면 된다.
- 공시 본문은 iframe이라 발동 시각을 파싱하지 않았다.
  - 우선 RSS `pubDate`를 발동 시각으로 쓰는 안
  - 공시가 실제 발동보다 늦게 올라올 수 있다 (지연 미측정).
- `_kind_items()`는 제목 앞 `[...]`를 떼고 시장을 `[코]` 접두어로만 판정한다.
  - 사이드카·CB는 원문 제목의 "유가증권시장/코스닥시장" 문구로 시장을 따로 판정해야 한다.

**구현 안 (추가 KRX 호출 없음)**
- `circuit_breaker.py` 폴링 루프에서 `domestic_news._kind_items()` 캐시를 재사용한다.
- 제목에 `사이드카(Side car) 발동`이 있으면:
  - `sidecar{available:true, active, market, direction(매수/매도), triggered_at}`
  - 사이드카는 5분 효력정지라 `triggered_at + 5분`까지 active로 둔다.
- 제목에 `CB 발동`·`매매거래 일시중단`이 있으면:
  - 새 필드 `circuit_breaker{active, market, stage, triggered_at}`
  - 20분 정지 기준
- 프론트: `js/skin-main.js`의 `homeCircuitBreakerBadges` IIFE
  - 사이드카 렌더는 이미 있음 (빨강 알약, 발동 펄스, 해제 5초)
  - CB 알약을 추가한다.
- 테스트는 위 실제 제목 문자열로 작성한다.
- CLAUDE.md의 `/api/circuit-breaker` 스키마 줄을 갱신한다.

## 6. 비용·인프라
- 9/15 평가판에서 유료로 전환함 (크레딧 약 ₩445,001, 평가판 약 9/21 종료)
- 예산 알림: "비용알림" ₩100, 50/90/100%
  - 크레딧 기간엔 ₩0으로 계산돼 알림이 오지 않는다.
- 9/1~13 크레딧 적용 전 요금:
  - VM(E2 Core·Ram) ₩3,393
  - 트래픽 ₩228
  - 9월 예상 약 ₩7,013
  - VM에 무료 한도가 거의 반영되지 않은 원인은 미확인
- **9/16~17**: 결제 → 보고서 일별 그래프에서 E2 요금이 0 근처로 떨어졌는지 확인
  - 안 떨어지면 결제 지원 문의 또는 오라클 이전 검토
- 권하지 않은 것:
  - Cloudflare: API가 캐시 불가 데이터이고, 요청 제한이 `request.client.host` 기준
  - Standard 네트워크 등급: 외부 IP가 바뀌어 키움 IP 등록이 깨짐
  - 둘 다 절약 효과가 월 ₩500 수준
- GCP 장애 LBDPMSS (E2 VM 생성 실패, 9/15 02:37~) 진행 중
  - 해결 전 VM 중지·시작·리셋 금지

## 7. 보류 과제
**오라클 이전** ("천천히", 비용 확인 결과에 따라 결정)
- 오라클 쪽 조건:
  - 7일 유휴 회수 위험 (종량제로 전환하면 회피)
  - 한국 리전 용량 부족 사례
  - 리전 변경 불가
- 우리 쪽 준비물:
  - 키움·NCP IP 재등록
  - KIS WebSocket은 앱키당 세션 1개라 두 서버 동시 운영 불가
  - 서버 설정이 저장소에 없음: 패키지 목록, HTTPS 앞단(uvicorn은 8080), systemd 유닛
  - `/home/goodbyestarwars` 하드코딩 3곳
  - SQLite 약 198MB·`.env` 이전
  - 오라클 방화벽 두 곳 개방
  - DNS·GAS 주소 확인
  - 전환 후 GCP VM·IP 삭제

**기타**
- 스캔 00:00 이동: 보류
  - `scan_forward.today_kst` 날짜 키가 하루 밀리고, 00:00 뉴스 배치와 겹친다.
- 거래량 돌파 후보 확대: KIS 순위 API 다음 페이지(`tr_cont`)나 `FID_VOL_CNT` 필터
  - 한 번에 오는 건수는 미확인
