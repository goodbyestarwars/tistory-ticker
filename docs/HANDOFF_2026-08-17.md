# 작업 인수인계서 — 2026-08-20

## 1. 현재 운영 상태

- 운영 브랜치: `origin/master`
- 최신 운영 커밋: `44794f2 Fill US board market caps across ranking tabs`
- 운영 health: `https://goodbyestar.cloud/health` → HTTP 200
- 작업 브랜치: `agent/weekly-report-cache-v4`
- 작업 브랜치 최신 커밋: `b778876 Fill US board market caps across ranking tabs` (운영 push에는 최신 master 기준 cherry-pick 커밋 `44794f2` 사용)
- 현재 작업 트리: clean

주의: 작업 브랜치는 오래된 기준 커밋에서 분기되어 `origin/master`와 이력이 다르다. 다음 배포 때 작업 브랜치 전체를 `master`에 강제 push하지 말고, 반드시 최신 `origin/master`에서 임시 worktree를 만든 뒤 필요한 커밋만 cherry-pick한다.

## 2. 최근 반영 내용

### 전체 디자인 시스템

- `style.css`에 전역 폰트·색상 토큰 적용
- 제목/뉴스/해석: MaruBuri
- UI/본문/메뉴: Nanum Gothic
- 숫자/시세/테이블: Malgun Gothic/system-ui + tabular numbers
- 홈·뉴스·시장지표·실시간 종목판·ETF/배당·종목분석·캘린더·커뮤니티의 공통 패널을 경제신문·리서치 화면 톤으로 정리
- 민트/네온 배지, 과한 그림자와 둥근 카드 스타일 축소

### 실시간 종목판

- 업종 TOP 활성 시 업종명 열을 34%로 확대
- 나머지 열도 14/15/10/13/14%로 배분
- 일반 순위 탭과 모바일 가로 스크롤은 유지
- 운영 반영 커밋: `f2b4fca`

### 경제 종합뉴스·홈

- 속보는 최신 운영본에서 5초 간격으로 한 건씩 전환
- 일반 뉴스 영역 내부 스크롤 유지
- 관심종목 주간 공시는 최신 운영본에서 DART 원문 팝업으로 표시
- 실적/배당 기준연도 필드 유지
- 메인 `휴장` 탭은 홈 위젯 재구성 과정에서 지면이 삭제되지 않도록 `home-closed-page`를 별도 보존한다.

### 차트검색 결과 없음 문구

- 실제 GAS 응답에는 결과가 정상 존재함을 확인했다.
- 확인 응답: `scannedAt=2026-08-16T20:33:55.75076+09:00`, `scanned=2368`, `universe=3913`
- `js/pattern-scan.js`의 `?patternScan=1` 요청에 timestamp query를 붙여 오래된 빈 응답 캐시 재사용을 차단했다.
- 문구를 `GAS에서 scanChartPatterns를 한 번 실행해야 함`에서 `VM 일일 스캔이 한 번 완료되면 표시됩니다.`로 변경했다.
- 운영 반영 커밋: `e0e69e0`

### 2026-08-19 후속 반영

- 국내 실시간 종목판에 거래증가율·거래회전율·거래대금회전율, 52주 고가·저가, 시가총액 필드를 보강했다.
- 홈에 주말 자동 휴장 지면과 `한국증시·미국증시·휴장` 탭을 추가했다. 휴장 탭은 API 응답을 기다리지 않고 즉시 전환한다.
- 우측 상단 글쓰기 연필은 iframe 모달 대신 최상위 Tistory 관리자 글쓰기 주소로 이동해 카카오 로그인 리다이렉트를 허용한다. 기존 스킨 HTML의 오래된 `onclick`도 `skin-shell.js`가 제거한다.
- 운영 반영 커밋: `fd5cfb9`, `/health`: 200, GitHub Pages 정적 JS 반영 확인.

### 2026-08-20 미국 실시간 종목판 시가총액 누락 수정

- 사용자 화면에서 미국 실시간 종목판 `거래대금` 탭은 `MRNA`, `MU`처럼 티커만 표시되고 `시가총액`이 `-`로 표시되지만, `시가총액` 탭에서는 `Moderna Inc`, `$25.1B`처럼 정상 표시되는 불일치를 확인했다.
- 원인은 회사명 차이가 아니라 KIS 해외 순위 TR이 지표별로 응답 필드를 달리하기 때문이다. 거래대금·거래량 순위 응답에는 종목코드·시세·거래량만 있고 영문명과 `tomv`(백만 USD 단위 시가총액)가 없으며, 시가총액 순위 응답에만 이 메타데이터가 있었다.
- `scripts/cloud-vm/market_board.py`에 `_merge_us_kis_metadata()`를 추가해 시가총액 순위 데이터를 티커(`symbol`) 기준으로 거래대금·거래량·등락률 등 다른 순위 섹션에 병합했다. 회사명으로 조인하지 않아 `MRNA`/`Moderna Inc`처럼 공급자별 이름이 달라도 안정적으로 매칭한다. 티커만 있던 이름은 정식 영문명으로 보강하고, `market_cap`은 기존 값이 없을 때만 보강한다.
- `test/test_market_board.py`에 MRNA 거래대금 행과 시가총액 행을 분리한 회귀 테스트를 추가했다. `display_name=Moderna Inc`, `market_cap=25100`(백만 USD) 병합을 검증한다.
- `docs/WORK_HISTORY.md`에도 동일 작업을 기록했다. 이번 변경은 GAS가 아니라 VM FastAPI의 `market_board.py` 변경이므로 GAS 재배포는 필요 없다.
- 전체 회귀 테스트 `370건` 통과, `python -m py_compile scripts/cloud-vm/market_board.py` 통과.
- 작업 브랜치 커밋: `b778876`. 최신 `origin/master` `4853cc9`에서 임시 worktree `C:\Users\goodb\Documents\Codex\2026-08-17\deploy-next`를 만들고 필요한 커밋만 cherry-pick해 `44794f2`로 `origin/master`에 push했다. 강제 push하지 않았다.
- push 직후 `/health`는 HTTP 200이었다. `https://goodbyestar.cloud/market-board?market=us&limit=20&fresh=1`가 여전히 `MRNA.display_name=MRNA`, `market_cap=null`을 반환하는 상태를 확인했으므로 VM 자동 배포/서비스 재시작 완료 후 반드시 다시 확인해야 한다. VM 자동 배포는 약 5분 걸릴 수 있다.
- 재확인 명령:

```powershell
$j = Invoke-RestMethod 'https://goodbyestar.cloud/market-board?market=us&limit=20&fresh=1'
$j.data.rows | Where-Object symbol -in @('MRNA','MU') | Select-Object symbol,display_name,market_cap
Invoke-WebRequest -UseBasicParsing https://goodbyestar.cloud/health
```

- 기대 결과: `MRNA`가 `Moderna Inc`로 표시되고 `market_cap`이 null이 아니어야 한다. 반영 후에도 null이면 VM `deploy_check.sh`의 git pull/restart 로그와 `market_board.py` 실행본을 확인한다.

## 3. 검증 결과

- 최신 운영본 기준 UI 테스트: 휴장/글쓰기 인증 신규 계약 테스트 통과
- 로컬 전체 회귀 테스트: 92건 실행, 기존 버전 문자열 계약 테스트 2건 외 통과
- JavaScript 문법 검사 통과
- Python 컴파일 검사 통과
- 업종 TOP 브라우저 실측: 6개 열 표시, 수평 넘침 없음
- 운영 `/health`: 200

## 4. 배포 절차

```powershell
$repo = 'C:\Users\goodb\Documents\Codex\2026-08-15\https-github-com-goodbyestarwars-tistory-ticker\work\tistory-ticker'
Set-Location $repo
git fetch origin master
git status --short
```

작업 브랜치가 최신 master와 다르면:

```powershell
$deploy = 'C:\Users\goodb\Documents\Codex\2026-08-15\deploy-next'
git worktree add $deploy origin/master
git -C $deploy cherry-pick <필요한-커밋>
python -m unittest discover -s test -p "test_*.py" -q
git -C $deploy push origin HEAD:master
git -C $repo worktree remove $deploy
```

운영 push 후 확인:

```powershell
Invoke-WebRequest -UseBasicParsing https://goodbyestar.cloud/health
git ls-remote origin refs/heads/master
```

사용자 요청상 작업 완료 후 merge/deploy까지 진행하는 흐름이다. 단, `master`를 강제로 덮어쓰지 않는다.

## 5. 수동 반영이 필요한 영역

- `skin.html`은 저장소에 반영되어도 Tistory 관리자 스킨 편집 화면과 자동 동기화되지 않는다. 실제 Tistory 스킨 HTML을 바꿀 때는 관리자 화면에 수동 붙여넣기가 필요하다.
- `gas/ticker-proxy.gs`를 수정한 작업은 Apps Script 편집기에서 새 버전 배포가 필요하다. 이번 차트검색 빈 캐시 수정은 프론트 `js/pattern-scan.js` 변경이라 GAS 재배포는 필요하지 않다.
- GitHub Pages 정적 `js/`, `css/`, `style.css`는 `master` push 후 반영된다.
- `scripts/cloud-vm/` 변경은 VM 배포 절차와 health 확인이 필요하다.

## 6. 다음 작업 시작 전 확인

1. `git fetch origin master` 후 운영 커밋을 기준으로 작업한다.
2. 사용자 화면에 오래된 차트검색 문구가 계속 보이면 `Ctrl+F5`를 먼저 안내한다.
3. GAS `patternScan=1` 응답에 `scannedAt`, `scanned`, `patterns`가 있는지 확인한다.
4. API가 다시 빈 응답이면 프론트 문제가 아니라 VM `daily_scan.py` 생성 파일/타이머와 GAS의 VM 호출 인증을 확인한다.
5. 데이터·API·분석 로직을 바꾸는 작업인지, 표현만 바꾸는 작업인지 먼저 구분한다.
