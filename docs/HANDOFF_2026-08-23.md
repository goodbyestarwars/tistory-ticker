# 작업 인수인계서 — 2026-08-23

## 1. 현재 운영 상태

- 운영 브랜치: `origin/master`
- 최신 커밋: `cfc63d4`(작업이력 기록). 실질적 최신 변경은 `0dac4e0`(전략검색 목표주가
  괴리 표 3열 분리, 14차). 목표주가 괴리 계산법 재설계는 `650b2c8`(12차, 아래 2절) -
  **VM 재실행으로 라이브 검증까지 완료됨.**
- 작업 트리: clean (모두 push 완료)
- `js/`·`css/`·`data/`: GitHub Pages 자동 반영(최대 10분)
- `scripts/cloud-vm/`: VM `kiwoom-deploy.timer`(약 5분 주기)가 자동 배포. **VM에 이미
  자동배포 파이프라인이 있어 사람이 직접 `git pull`할 필요는 보통 없지만, 아래 "진행
  중이던 작업" 항목의 재실행 확인은 필요.**
- 이 세션에서 반복적으로 겪은 문제: VM `deploy_check.sh`가 "이전 배포/점검이 아직
  진행 중"이라며 자기 자신을 스킵하는 상황 - 좀비 자식 프로세스(주로 `strategy_scan.py`/
  뉴스 백필 스크립트)가 오래 붙어있을 때 발생. `ps aux | grep <script>` -> 좀비 kill ->
  `sudo systemctl start kiwoom-deploy.service`로 강제 트리거하면 풀림.

## 2. 진행 중이던 작업 — **완료됨(4단계 재설계까지 VM 라이브 검증 끝남)**

**목표주가 괴리 저평가주 - 4단계(방법론 재설계), VM 라이브 검증 완료.**

3단계까지(중앙값+PER/PBR 캡+괴리율 300% 상한)로도 사용자가 라이브에서 KG스틸(자체계산
19,957원/+277.3% vs 실제 애널리스트 목표가 7,550원/+42.7%) 등 여러 종목이 실제 애널리스트
목표가를 몇 배씩 웃도는 것을 재차 지적("계산식이 이건 애초에 성립하면 안 되는 목표가야").
캡을 더 죄는 대신 계산법 자체를 바꿨다(4단계, `650b2c8`):
- `TARGET_PRICE_REVERSION_FACTOR`(0.35) - 섹터 중앙값까지 "완전수렴"이 아니라 현재
  배수에서 그 방향으로 35%만 부분수렴한다고 가정. `TARGET_PRICE_MAX_GAP_PCT`도 300→150.
- `apply_analyst_target_price_ceiling()` 신설 - 같은 종목에 실제 애널리스트 목표가
  (`analystTargetPrice`)가 있으면 자체계산값이 그걸 절대 넘지 않도록 클램프.
- 로컬 테스트(566건, 신규 4건 포함) 전부 통과.
- **VM에서 `strategy_scan.py` 재실행 후 라이브로 검증 완료(2026-08-23)**: 최상위 종목
  (삼지전자) 괴리율 130.3%로 새 상한(150%) 안에 정상 진입. 세아제강지주는 자체계산
  186,955원(+86.4%)이 클램프 없이도 이미 애널리스트 목표가(205,000원)보다 낮게 나옴.
  현대지에프홀딩스는 자체계산이 애널리스트 목표가(20,000원)와 정확히 같아지며
  `targetPriceCappedByAnalyst` 플래그가 실제로 붙는 것도 확인. **정상 동작으로 결론.**

## (참고) 이전 3단계 — VM 라이브 검증까지 끝난 상태

**목표주가 괴리 저평가주 - 이상치 수정, 3단계 전부 완료·VM 라이브 확인까지 끝남.**

- **1단계(완료)**: 섹터 평균을 산술평균 → 중앙값(median)으로 교체(`efdac68`).
  효성화학 괴리율 +17831% → +1717%로 줄었지만 여전히 비정상.
- **2단계(완료, VM 라이브 검증 끝남)**: 커밋 `4a8ac0e`에서 두 겹의 추가 방어 -
  ① `TARGET_PRICE_PER_CAP`(80)/`TARGET_PRICE_PBR_CAP`(10)를 넘는 개별 종목은 섹터
  비교 표본 자체에서 제외 ② 계산된 괴리율이 `TARGET_PRICE_MAX_GAP_PCT`(300%)를
  넘으면 후보에서 제외. **VM에서 `strategy_scan.py` 재실행 후 실측 확인 완료 —
  최상위 종목 괴리율이 +297.8%로 상한(300%) 안에 정상적으로 들어옴.** 정상 동작으로
  결론.
- **3단계(완료, 사용자 요청에 따른 코드베이스 전체 감사)**: 사용자가 "계산이 하나
  틀렸으면 다른 것도 신뢰 문제 생길까봐"라며 `scripts/cloud-vm/*.py` 전체에서 같은
  패턴(여러 종목/리포트를 산술평균)이 더 있는지 점검 요청. `invest_signal.py` 종합
  점수(컴포넌트별 사전 클램프+가중합, 안전 확인)와 각종 시계열 지표(단일 종목 자기
  값, 안전)는 문제없음을 확인. `invest_opinion.py`의 "평균 목표가"(여러 증권사 리포트
  목표가 평균)에서 같은 위험(액면분할 등으로 스케일 다른 옛 리포트가 섞이면 왜곡)을
  추가로 발견해 선제적으로 중앙값으로 교체(`553f8bb`, 실제 장애 확인된 건 아니고
  예방 조치). 회귀 테스트 추가 후 전체 스위트 562건 통과(기존 Windows 환경 무관
  실패 5건은 그대로).
- **남은 미결 사항(사용자가 명시적으로 보류)**: 300% 상한이 여전히 관대한지("몇 배
  PER 낮은 게 기준인지 없다")는 사용자가 질문했으나 **AskUserQuestion을 두 번 다
  응답 없이 넘겼다** - 다음에 다시 꺼내기 전엔 상한값(80/10/300)을 임의로 더 조이지
  말 것. 근본적으로 "최근 1개년 net_income 그대로 사용"(다년 정규화 안 함)이라는
  한계가 있어 완벽한 차단은 어렵다는 점을 전제로 설명할 것.
- 이 세션 도중 **동시에 여러 `strategy_scan.py`/`kiwoom-deploy` 프로세스가 겹쳐 도는
  사고가 여러 번 났다**(사용자가 같은 명령을 실수로 두 번 붙여넣거나, 자동배포 타이머와
  겹침). 재실행 지시 전에 항상 `ps aux | grep strategy_scan.py`로 먼저 확인할 것 -
  중복 실행은 캐시 파일을 서로 덮어써 결과가 뒤죽박죽될 수 있다.

## 3. 이번 세션에서 반영된 주요 내용 (커밋 역순)

1. **전략검색 목표주가 괴리 표 - 전략 지표를 괴리율/목표가/애널 목표가 3열로 분리**
   (`0dac4e0`, 14차) - 사용자가 스크린샷으로 "3개 칸으로 나눠서 구분해줘" 요청. 기존엔
   "전략 지표" 한 칸에 자체계산 목표가(괴리%)와 애널리스트 목표가가 줄바꿈으로 겹쳐
   있어 알아보기 어려웠음. `targetPriceGap` 카테고리에서만 3열로 분리(다른 카테고리는
   신호 형태가 달라 기존 1열 유지), `<th>`에도 `<td>`와 동일한 열 클래스를 달아 정렬
   CSS를 위치(nth-child)가 아니라 클래스(의미) 기준으로 전환(국민연금 표도 회귀 없게
   같이 수정). 로컬 테스트 하네스로 데스크톱/모바일 둘 다 실측.
2. **전략검색 모바일 카드 line-height 수정**(`da662a0`, 13차) - 사이트 전역 본문
   line-height(~1.9)를 물려받아 필드 사이가 헐렁하게 벌어져 보이던 문제, 1.45로 통일.
3. **코드베이스 전체 신뢰성 감사 + invest_opinion 선제 수정**(`553f8bb`) - 아래 4번
   버그를 계기로 사용자가 "다른 계산도 신뢰 문제 있을 수 있으니 살펴봐"라고 요청,
   `scripts/cloud-vm/*.py` 전체의 산술평균 사용처를 점검. `invest_opinion.py`의 여러
   증권사 리포트 목표가 평균을 중앙값으로 교체(예방 조치, 실제 장애 미확인). 그 외는
   전부 안전 확인. 상세는 위 2절 3단계 참고.
4. **효성화학 이상치 버그**(`efdac68` → `4a8ac0e`, **VM 라이브 검증 완료**) - 섹터 평균
   PER/PBR을 `sum/len` → `statistics.median`(1단계)으로 바꿨는데도 여전히 비정상이라,
   PER/PBR 상한을 넘는 개별 종목을 표본에서 제외하고 계산된 괴리율 자체에도 상한
   (300%)을 거는 2단계 방어를 추가(`4a8ac0e`). VM 재실행 후 최상위 종목 +297.8%로
   상한 안에 정상 진입함을 실측 확인. `efdac68` 같은 커밋에서 평균 투자의견 막대의
   `otherCount` 누락(퍼센트 합 100% 안 됨)과, 순매매량/보유율 차트를 한 줄에 배치하며
   생긴 높이 불일치(`FOREIGN_RATIO_H` 신설)도 같이 수정. 최종 4단계 재설계(부분수렴 +
   애널리스트 상한)는 위 2절 참고 - `650b2c8`, 12차.
5. **평균 투자의견 카드**(`b0da046`, `f715214`, `c8bc1fd`, `afac8d7`) - 토스증권의 "최근
   3개월 애널리스트 평균 투자의견"을 참고해 KIS `국내주식 종목투자의견`(`FHKST663300C0`)
   으로 구현. 처음엔 종목분석 펀더멘탈 탭에 넣었다가 사용자 피드백으로 탭 밖 "요약"
   영역(`.ff-summary-card`)으로 이동. 차트검색/전략검색에는 라이브 호출 대신 하루 1회
   DB 배치(`db_schema.invest_opinions` 테이블, `invest_opinion.
   enrich_matches_with_target_price()`)로 "애널리스트 목표가"를 붙임 - 전략검색의
   `targetPriceGap` 카테고리가 이미 쓰는 자체 목표가(`targetPrice`/`targetGapPct`,
   업종 PER/PBR 기반)와 필드명이 겹치지 않게 `analystTargetPrice` 등으로 분리했다.
6. **옵션 수급 행사가별 프로파일 항상 비어있던 버그**(`8a7bee8`) - `option_flow.py`가
   행사가 필드를 추정 명칭 목록으로 찾다가 실제 필드명(`acpr`, KIS 공식 문서로 확인)이
   빠져 있어 전 행이 걸러졌었다. `kis-code-assistant-mcp`로 공식 예제를 찾아 확인 후 수정.
7. **차트 레이아웃**(`93494fd`) - 순매매량/보유율 차트를 반 폭씩 한 줄에 배치.
8. **차트검색 신규 패턴**(`a03ddcb`) - "단기이평 돌파형"(하락 추세선을 종가+5일선이
   함께 돌파). `pattern_detect.py`의 기존 `scan_stock()` 루프에 편입, GAS 미러 불필요.
9. **코스피·코스닥 주간현물 분봉 온디맨드 분리**(`1631b8d`) - 기본 응답에서 분봉(1,500봉
   x2종목)을 빼고 `/domestic-market-indicators/chart` 신규 엔드포인트로 분리해 페이지
   로드 속도 개선.
10. **목표주가 괴리 저평가주 계산법 전면 교체**(`17e02e4`, `c0e9307`, `7253c15`, `63d1675`)
   - 원래 "종목 자체 과거 5개년 PER/PBR 밴드" 설계였으나 `daily_prices`가 2024-06-24
   부터만 있어(완결 회계연도 2개뿐) 항상 후보 0건이었던 것을 발견 → "업종 평균(현재는
   중앙값) PER/PBR 대비" 방식으로 재설계.
11. 그 외: 증시온도 빚투위험도 pending 고착 수정(`973b0c9`), ⓘ 툴팁 잘림 수정(`d6523a5`),
   휴장 탭 `$심볼` 표기 제거(`59a4609`), 심장박동 스피너 색상(잠깐 초록 → 파랑으로 원복,
   `6f32b09`/`4501c90`).

## 4. 코딩할 때 주의할 점 (이번 세션에서 얻은 교훈)

### 데이터/계산 관련

- **섹터·업종 단위로 여러 종목의 배수(PER/PBR 등)를 집계할 때는 절대 단순 산술평균을
  쓰지 말 것.** 이익이 0에 가까운 종목 하나만 있어도 PER이 수천 배로 튀어 평균 전체를
  왜곡한다(효성화학 사례). **중앙값(median)을 기본으로 쓰고**, 정말 평균이 필요하면
  이상치를 먼저 걸러내거나(예: PER 상하위 N% trim) 그 이유를 주석에 남길 것.
- **KIS/키움 API 응답 필드명은 절대 추측하지 말 것.** 이번 세션에 두 건이나
  "그럴듯한 이름을 여러 개 나열해 순서대로 시도"하다가 실제 필드명이 목록에 없어서
  기능이 통째로 죽어있던 사례가 나왔다(`option_flow.py`의 `acpr`, 이전 세션의 다른 TR도
  유사). `kis-code-assistant-mcp`(user-scope 설치됨, `search_domestic_*_api` +
  `read_source_code`)로 공식 예제의 `COLUMN_MAPPING`을 반드시 먼저 확인할 것 - 이
  MCP는 코드 검색 전용이라 실호출은 못 하지만, 필드명 자체는 신뢰할 수 있는 소스다.
- **상대가치평가(섹터/업종 배수를 곱해 목표가를 내는 방식)는 "완전수렴"을 가정하면
  안 된다.** 실적이 눌린(트로프) 종목의 낮은 배수를 업종 평균/중앙값까지 그대로
  밀어올리면 캡을 아무리 죄어도 비현실적인 숫자가 나온다(목표주가 괴리 사례, 캡으로
  세 번 막았는데도 실제 애널리스트 목표가 대비 몇 배씩 높았음). "부분수렴"(현재값과
  목표값 사이를 일정 비율만 잇는다) 가정이 훨씬 현실적이고, 실제 참조값(애널리스트
  목표가 등)이 있으면 그 값을 최종 상한으로 삼아 이중으로 검증할 것.
- **단위 혼동에 항상 주의.** 이 코드베이스에는 "억원 단위" vs "원 단위", "백만원" vs
  "원" 혼동으로 인한 버그가 반복적으로 나왔다(`market_cap_eok` 명명 규칙 참고). 새 KIS/
  키움 필드를 쓸 때는 실측값으로 자릿수가 말이 되는지 검증할 것.

### 배포/운영 관련

- **VM에서 스크립트를 재실행하라고 안내하기 전에 항상 `ps aux | grep <script명>`으로
  이미 도는 게 없는지 확인할 것.** 이 세션에서 중복 실행 사고가 5번 이상 났다(사용자가
  명령을 두 번 붙여넣거나, 자동배포 타이머와 겹침). 중복 실행은 같은 캐시 파일을 서로
  덮어써 결과를 예측 불가능하게 만든다.
- **`kill <PID>`가 항상 프로세스를 죽이는 게 아니다.** `nohup ... &`로 띄운 셸 job의
  PID를 죽여도 그 자식 프로세스(실제 `python ...`)가 살아남는 경우가 있었다. `kill`
  직후 반드시 `ps aux`로 재확인할 것.
- **VM 코드 변경 후 라이브로 확인하기 전에 파일이 실제로 배포됐는지 먼저 확인할 것.**
  `grep -n "<새로_추가한_함수명>" ~/kiwoom-api/<파일>.py`로 배포 여부를 확인하고,
  안 됐으면 `sudo systemctl start kiwoom-deploy.service`로 강제 트리거하거나 좀비
  프로세스부터 정리한다(위 1절 참고).
- **FastAPI 새 엔드포인트를 추가한 직후엔 서비스 재시작이 필요하다.** 파일만 배포되고
  `uvicorn` 프로세스가 안 살아나면 404가 계속 난다 - `sudo systemctl restart kiwoom-api`
  로 수동 재시작 후 확인.

### 프론트엔드 관련

- **필드명 충돌에 주의.** 전략검색의 `targetPriceGap` 카테고리는 이미 `targetPrice`/
  `targetGapPct`(업종 배수 기반 자체 계산)를 쓰고 있다 - 나중에 "목표가" 관련 필드를
  추가할 때 이 이름을 재사용하면 서로 다른 계산 결과가 뒤섞인다. 새 계산은 항상
  구분되는 접두어(`analyst*` 등)를 쓸 것.
- **두 요소를 한 줄에 나란히 배치할 때 SVG viewBox 비율이 다르면 높이가 안 맞는다.**
  `width:100%; height:auto`로 반응형 SVG를 쓰는 차트를 컬럼 나누기 하려면, 두 차트의
  viewBox 가로세로 비율을 맞추거나(이번엔 후자를 택함, `FOREIGN_RATIO_H` 신설) 최소한
  같은 폭일 때 결과 높이가 얼마가 되는지 미리 계산해볼 것. 공유 상수(`RATIO_H`)를
  손대면 그 상수를 쓰는 다른 화면(RSI 차트 등)에 의도치 않은 영향이 갈 수 있으니, 필요한
  범위에만 새 상수를 분리할 것.
- **매수/중립/매도처럼 카테고리 합이 전체와 같아야 하는 값은 "기타" 버킷을 반드시
  화면에도 반영할 것.** 분류 안 되는 값을 조용히 버리면 막대그래프 퍼센트 합이
  100%가 안 돼 사용자에게 "정렬이 이상하다"는 오해를 산다.
- Windows Git Bash에서 `python3 -c "..."` 멀티라인 heredoc은 자주 깨진다 - 스크래치패드에
  `.py` 파일을 써서 `python <path>`로 실행하는 편이 안전하다(로컬 진단용). VM 쪽은
  사용자가 직접 터미널에서 실행하므로 `cat > /tmp/x.py << 'EOF' ... EOF` 형태로 안내한다.
- **모바일 카드 레이아웃(`.ss-comparison-table` 계열)에서 `<td>`는 기본 `display:none`이고
  화이트리스트에 명시적으로 올린 클래스만 보인다.** 새 열 클래스를 추가했다면 반드시
  모바일 media query의 allowlist에도 그 클래스를 추가할 것 - 안 하면 데스크톱에서는
  보이는데 모바일에서만 조용히 사라진다(전략검색 3열 분리 때 실제로 이 단계를 빠뜨릴
  뻔함).
- **여러 카테고리가 같은 표 CSS 클래스(`.ss-strategy-table` 등)를 공유하면서 카테고리마다
  열 개수/의미가 다를 때, `th:nth-child(N)` 같은 위치 기반 정렬 CSS는 위험하다.** 한
  카테고리에서 열을 추가/삭제하면 다른 카테고리의 정렬이 같이 틀어진다(이번에
  targetPriceGap만 3열 늘리다가 발견) - `<th>`에도 `<td>`와 동일한 의미 클래스를 달고
  클래스 기준 선택자로 통일할 것. 국민연금 표처럼 같은 CSS를 재사용하는 다른 표도 있는지
  먼저 확인(`grep -n "ss-strategy-table" js/*.js`)하고 전부 클래스를 맞춰야 회귀가 안 난다.
- **한 칸에 두 값을 `<br>`로 줄바꿈해 넣는 방식은 값이 늘어나면 알아보기 어려워진다.**
  "목표가 (괴리) / 애널리스트 목표가"처럼 성격이 다른 수치를 한 셀에 합치지 말고, 데이터가
  구조적으로 분리 가능하면(필드가 이미 따로 있음) 처음부터 별도 열로 뽑는 편이 낫다.

## 5. 다음 작업 시작 전 확인

1. `git log -1`로 로컬이 `origin/master`와 같은지 확인(이 세션 종료 시점 `cfc63d4`).
2. 목표주가 괴리 재설계(4단계)는 VM 라이브 검증까지 이미 끝났다 - 재검증 불필요.
3. **미해결로 남은 것**: 사용자가 전략검색 모바일 카드 레이아웃에 대해 "표로 안 보이고,
   상단이 고정돼서 안 올라간다"고 리포트했으나, 자동 점검(375px 뷰포트)에서는 재현이
   안 됐고 사용자도 후속 스크린샷 없이 다른 요청(목표주가 괴리 3열 분리)으로 넘어갔다 -
   진짜 버그인지, 일시적 로딩 지연이었는지, 다른 페이지 얘기였는지 불명. 다음 세션에서
   사용자가 다시 언급하면 페이지 URL과 스크린샷부터 확보할 것.
4. VM 작업을 지시하기 전엔 항상 기존 프로세스 유무를 먼저 확인한다.
5. `CLAUDE.md`의 "작업별 문서 참조 규칙" 표를 참고해 관련 문서를 먼저 읽는다.
6. 새로 "여러 종목/리포트를 평균 내는" 계산을 추가할 때는 처음부터 `statistics.median`을
   기본으로 쓸 것(이번 세션의 핵심 교훈, 4절 참고) - 이미 두 곳에서 산술평균 때문에
   같은 종류의 버그가 났다.
7. 여러 카테고리가 같은 표 CSS를 공유하는 화면(전략검색 등)에서 열 구조를 바꿀 땐
   4절의 "위치 기반 CSS 위험" 항목을 먼저 읽을 것.

## 6. 2026-08-23 후속 개선(현재 작업 트리)

- **목표주가 괴리 계산 후속 수정**: 기존 `apply_analyst_target_price_ceiling()`은 자체
  목표가가 KIS 애널리스트 목표가보다 높을 때 애널리스트 값을 그대로 복사해 두 값이
  같은 종목이 많아지는 문제가 있었다. `apply_analyst_target_price_anchor()`로 바꾸고,
  현재가에서 애널리스트 목표가까지의 70%만 참고하는 소프트 앵커로 변경했다. 자체
  목표가가 애널리스트보다 낮으면 기존 계산을 유지하며, 애널리스트 목표가가 현재가보다
  낮은 종목은 제외한다. 애널리스트 커버리지가 없어도 자체 괴리율은 60% 상한을 넘지
  않는다. 자체 목표가와 애널리스트 목표가는 계속 별도 열이다.
- **종목분석 메인 안내 영역**: 흐름 미선택 안내(`.ff-flow-empty`)의 과도한 패딩과
  높이를 줄여 흐름 카드들과 같은 밀도로 맞췄다. 데스크톱·390px 모바일 로컬 화면에서
  카드 2열과 안내 영역을 확인했다.
- **과거 시뮬레이션 평단 입력**: `내 평단`을 입력하면 과거 재생 그래프와 분리된
  최신 종가 기준 손익률·주당 차이를 즉시 표시한다. 평단이 없을 때는 입력 안내만
  보이며 기존 투자금 시뮬레이션은 그대로 동작한다.
- **MY 밀도 개선**: 큰 글자·두꺼운 테두리·넓은 패딩을 덮어쓰던 확장 스타일을
  컴팩트하게 조정했다. 핵심 수치의 가독성은 유지하고 카드 사이 빈 공간을 줄였다.
- 목표주가 계산·표시 변경은 `797e086`까지 `master`에 커밋·push했다. VM 배포 후
  목표주가 관련 전략 스캔은 새 배치 실행이 필요하다.

## 7. 2026-08-23 목표주가 신뢰·현실성 기준 표시

- 전략검색의 목표가 열을 `참고 목표주가`로 명확히 하고 각 값 아래 `6~12개월 참고`를 표시했다.
- 방법론 요약과 VM 방법론에 업종 PER·PBR 중앙값, 35% 부분수렴, 애널리스트 70% 앵커,
  괴리율 60% 상한을 함께 적어 동일한 계산 기준을 화면에서 확인할 수 있게 했다.
- 과거 적중률 백테스트가 끝난 확정 예측값이 아니라 참고용 근사치라는 검증 상태를 명시했다.
