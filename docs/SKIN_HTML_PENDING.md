# skin.html 미반영 항목

`skin.html`은 티스토리 관리자(스킨 편집기)에서만 고칠 수 있다. git push로는 운영 화면에
닿지 않는다. 이 문서는 **리포의 `skin.html`은 이미 맞는데 운영 스킨이 낡아서 생기는
문제**와, 그것 때문에 임시로 넣어둔 런타임 우회를 모아 둔다.

## 규칙

- `skin.html`을 바꾸면 해결되는 문제에 **런타임 우회를 새로 만들지 않는다.** 여기에
  기록만 하고 사용자에게 알린다(2026-09-05 사용자 지시: "html 바꾸면 되는 문제는
  기억만해, 땜질식으로 수정할 필요없어").
- 이미 들어가 있는 우회는 지우지 않는다. 지금 운영 화면이 그걸로 버티고 있어서,
  먼저 지우면 반영 전까지 화면이 나빠진다. 반영 뒤에 함께 걷어낸다.
- 티스토리가 **매 요청마다 주입**하는 것(파비콘 link 등)은 여기 해당하지 않는다.
  스킨을 갱신해도 계속 주입되므로 런타임 처리가 정상 해법이다.

## 반영 방법

티스토리 관리자 → 스킨 편집 → HTML에 이 리포의 `skin.html`을 붙여넣고 저장.

## 지금 밀려 있는 것 (2026-09-05 기준)

| 항목 | 운영 스킨 상태 | 리포 상태 | 지금 버티는 방법 |
|---|---|---|---|
| 브랜드 로고 참조 | `img/heart-monitor.svg` (옛 컬러 마크 경로) | `img/brand-banner.png` · `img/favicon.png` | `heart-monitor.svg` **파일 내용**을 현재 로고로 바꿔 첫 그림부터 맞게 했다 |
| 네비 로고 `<img>` | 옛 경로가 박힌 `<img>` | 새 경로 | `style.css`가 `.nav-logo-emblem` 배경으로 그리고 `img`는 `display:none` |
| 로고 텍스트 | 옛 문구 | `ㄱㅖ조 ㅏ심폐소생술` | `js/skin-menu.js`가 런타임에 덮어씀 |
| 옛 MY 아이콘 · 폰트 전환 버튼 | 남아 있을 수 있음 | 없음 | `js/skin-menu.js`가 런타임에 제거 |
| `apple-touch-icon` link | 없음 | 있음 | `js/skin-shell.js`가 런타임에 추가 |

### 반영하면 같이 걷어낼 수 있는 것

- `img/heart-monitor.svg` — 아무도 참조하지 않게 되므로 삭제 가능(옛 컬러 벡터는 git
  이력 `079c122`에 있다).
- `style.css`의 `.nav-logo-emblem img { display: none; }` — 다만 배경으로 그리는 방식
  자체는 유지하는 게 낫다. `<img src>` 교체가 없어야 로고가 깜빡이지 않는다.
- `js/skin-menu.js`의 로고 텍스트 덮어쓰기.

### 반영해도 남겨야 하는 것

- `js/skin-shell.js`의 파비콘·홈 화면 아이콘 link 교체. 티스토리가 `<head>`에 자기
  아이콘 link 3개를 **매번** 주입하므로(2026-09-04 실측), 스킨을 갱신해도 계속 걷어내야
  한다.
