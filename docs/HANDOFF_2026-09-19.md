# 인수인계 2026-09-19

## 0. 기본 정보
- 저장소: `goodbyestarwars/tistory-ticker`, 브랜치 `master`
- 로컬 클론: `C:\Users\goodb\Documents\Codex\2026-08-28\wlr\work\tistory-ticker`
- 운영 사이트: https://ghlee.tistory.com
- 운영 API: https://goodbyestar.cloud
- VM: GCP `instance-20260712-153038` / `us-central1-c` / e2-micro (메모리 1GB, 스왑 2GB)
- 최신 커밋: `aad92dcf Fix Ichimoku cloud fallback rendering`

## 1. 사용자 상시 지시
- 완료하면 묻지 말고 머지·배포한다. 완료 기준은 라이브 반영 확인까지다.
- 비용이 늘 수 있는 변경은 먼저 알린다.
- 스캔은 07:30 KST까지 끝낸다. 건너뛰지 않고 마감으로 끊는다.
- 왼쪽 색 막대(border-left 강조)는 사용하지 않는다. 배경 사각형 상자를 쓴다.
- 개미 관점으로 쓴다. 분할매수·현금 비중 같은 기관식 표현은 금지한다.
- 글보다 그림 먼저.
- 주식 이야기는 존댓말 대신 평서체를 사용하고, 중학교 1학년이 이해할 만큼 쉽게 쓴다.
- 단타 기능은 1초 싸움 관점으로 고도화한다.

## 2. 최근 작업: 실시간 시세 차트 일목균형표
사용자 제보: `실시간 시세 차트`에서 `일목균형표(구름) 표시`를 누르면 체크는 되지만 차트 영역에 아무것도 보이지 않음.

확인 결과:
- `js/stock-search.js`의 토글과 데이터 수신은 정상.
- `flowChart` 일봉 데이터는 010170 기준 500봉, OHLC 누락 없음.
- 선행스팬을 현재 봉에서 26봉 앞으로 이동하면서 `timeToCoordinate()`가 캔들 데이터에 없는 미래 날짜를 변환하지 않는 Lightweight Charts 환경이 있었고, 모든 구름 폴리곤이 좌표 검증에서 건너뛰어짐.

수정:
- `ichimokuCloudPoints()`가 `sourceTime: bars[i].date`를 함께 저장.
- `installIchimokuCloudCanvas()`가 미래 시각 좌표가 유한하지 않으면 sourceTime 좌표로 대체.
- 미래 좌표를 지원하는 환경에서는 기존 선행스팬 위치를 그대로 사용.
- 회귀 테스트에 fallback 토큰 검증 추가.

변경 파일:
- `js/stock-search.js`
- `test/test_ui_ia.py`

검증:
- `python -m pytest test/test_ui_ia.py -q`
- 결과: `157 passed, 23 subtests passed`
- GitHub `master`에 push 완료: `aad92dcf`
- GitHub Pages는 자동 배포되며 캐시 지연이 있을 수 있음. 반영 후 강력 새로고침으로 확인.

## 3. 배포·검증
- `js/`, `css/`, `style.css`, `legal/`, `learn/`: master push 후 GitHub Pages 자동 배포(캐시 약 10분).
- `scripts/cloud-vm/`, `data/`: VM `kiwoom-deploy.timer`가 5분마다 확인·복사·재시작.
- 전체 테스트 기준선은 HANDOFF_2026-09-17 기준 `1,081 passed`이며, 이번 변경은 UI 테스트만 재실행함.
- 장중(09:00~15:30 KST) VM 배포는 WebSocket을 끊을 수 있으므로 피한다.

## 4. VM 접속
PowerShell:
```powershell
$gc = "$env:LOCALAPPDATA\Google\Cloud SDK\google-cloud-sdk\bin\gcloud.cmd"
& $gc compute ssh instance-20260712-153038 --zone us-central1-c --strict-host-key-checking=no --quiet --command 'uptime; free -m'
```
주의:
- 원격 명령에 큰따옴표를 넣지 않는다(cmd가 인자를 쪼갬).
- 로그인 사용자는 `goodb`; 앱 폴더는 `sudo`가 필요하고 절대경로를 쓴다.
- git은 `sudo -u goodbyestarwars git`을 사용한다.
- VM의 스캔/전략검색은 1GB 메모리와 잠금(flock)을 전제로 한다. `dailyscan`과 전략검색을 동시에 실행하지 않는다.

## 5. 다음 창에서 먼저 확인할 것
1. Pages 반영 후 `https://ghlee.tistory.com/page/stock-search`에서 종목 검색 → 실시간 시세 차트 → 일목균형표 체크.
2. 일목 구름 색 영역과 캔들이 함께 보이는지 확인. 캐시가 남으면 Ctrl+Shift+R 또는 새 시크릿 창.
3. 여전히 빈 경우 브라우저에서 `.ss-ichimoku-cloud` canvas의 크기와 픽셀, Lightweight Charts `timeToCoordinate` 결과를 확인한다. 그때는 canvas z-index/투명 배경을 점검한다.
4. 사용자 요청이 없는 한 footer 등 이전 변경은 되돌리지 않는다.

## 6. 알려진 이슈·숙제
- `daily_cache = {code: load_daily_prices(...) for code in codes}`가 1GB VM에서 최대 513MB RSS/886MB swap을 사용한다. 코드별 순차 캐시로 줄이는 후속 작업 가치가 있다.
- 전략검색은 ETF를 매일 유지하고, 나머지는 주 1회(금요일 또는 월요일)로 바꾸는 방향을 사용자가 검토 중이다. 확정 전에는 스케줄을 바꾸지 않는다.
- 미국 종목 TR `usa10099` 경로 문제로 한글명·거래소 코드가 빠지고 야후 폴백 중인 별도 작업이 있다.
- Groq Compound deprecation 메일은 현재 저장소에서 Groq 모델을 쓰는지 별도 확인이 필요하다.
- Google Cloud Backup and DR API 자동 활성화 메일은 프로젝트 `project-e37adc95-8c74-4a6a-bab` 관련이다. 사용하지 않으려면 콘솔/API 상태와 비용 발생 여부를 확인한 뒤 비활성화 절차를 진행한다.
