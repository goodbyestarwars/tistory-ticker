# 백엔드 일원화 방향 — GAS 폐기, VM으로 수렴

작성 2026-08-31. 이 문서는 "왜 그렇게 가는가"와 "어떤 순서로 가는가"를 고정한다.
개별 작업 내역은 `docs/WORK_HISTORY.md`, 구조 현황은 `ARCHITECTURE.md`를 본다.

## 1. 문제 — 백엔드가 둘이고, 느린 쪽이 앞에 있다

`ARCHITECTURE.md`에 적힌 대로 브라우저가 직접 부르는 백엔드가 **GAS와 VM 2군데**다.
문제는 나뉘어 있다는 것 자체보다, **느린 쪽(GAS)이 빠른 쪽(VM) 앞에 서 있다**는 것이다.

2026-08-31 홈 첫 화면 실측(모바일 375px, 브라우저 안 기준):

| 호출 | 응답 |
|---|---|
| VM `/futures` | 306ms |
| VM `/investor-trend` ×2 | 301ms |
| VM `/market-board` | 262ms |
| VM `/watchlist`, `/auth/google/me` | 241ms |
| **GAS `?bubble=1`** | **3,708ms** |
| **GAS `?marketTemp=1`** | **7,102ms** |

VM 호출은 전부 1.34초에 끝나는데 **마지막 데이터는 7.9초에 도착한다**(load 이벤트
1.16초의 6.8초 뒤). 즉 홈의 체감 속도는 전적으로 GAS가 결정한다.
curl 단독 측정으로도 GAS는 캐시 히트 1.6~2.7초, 미스 7초+다. 브라우저 안에서는
다른 요청과 경쟁해 더 나빠진다.

## 2. 결정적 근거 — GAS는 이미 VM의 중간상이 됐다

느린 것보다 중요한 건 **토폴로지가 거꾸로**라는 점이다. GAS 코드가 스스로 VM을 부른다:

- `fetchKofiaMarketFromVm_()` → `kiwoomVmFetch_('/kofia-market?days=30')`
  (`getMarketTemp()`가 신용융자 점수를 낼 때 사용)
- `getFundamentals_()` → `kiwoomVmFetch_('/quote?code=')` 를 받아 필드만 재포장

지금 데이터 흐름:

```
브라우저 → GAS(1.6~7초) → VM(0.25초) + 외부(네이버 등)
```

여기서 GAS를 빼면:

```
브라우저 → VM(0.25초) + 외부
```

**한 홉이 통째로 사라진다.** GAS가 하는 일 중 상당수는 이미 VM이 가진 데이터의 재포장이다.

부수 근거:
- VM 엔드포인트가 이미 **50개**로 대부분 도메인을 덮는다. GAS에 남은 액션은 24개.
- 같은 도메인이 양쪽에 중복 존재한다: `action=foreignFlow` ↔ `/foreign-flow/{code}`,
  `action=fundamentals` ↔ `/fundamentals/{code}`.
- 이미 한 번 같은 판단을 내린 적이 있다 — `js/foreign-flow.js` 주석:
  "2026-07-13: GAS `?action=investorFlow` 경유 방식은 **폐기됨** - GAS→VM 구간이
  간헐적으로 통째로 막히는 원인 불명 현상 때문에 브라우저가 VM을 직접 호출".
  **마이그레이션은 이미 시작됐고 중간에 멈춰 있다.**

## 3. 방향

> **GAS를 새 기능의 경유지로 쓰지 않는다. 남은 GAS 액션은 VM으로 옮기고, 옮긴 것은
> GAS에서 지운다. 최종 목표는 브라우저 → VM 단일 경로다.**

명시적으로 **하지 않을 것**:
- **VM이 GAS를 프록시하고 캐시하는 방식은 쓰지 않는다.** 응답은 빨라지지만 GAS 의존이
  영구화되고, GAS↔VM 구간(이미 장애 이력이 있는 링크)이 상시 경로가 된다. 증상만 덮는
  땜질이다.
- GAS 캐시 TTL을 늘리는 식의 조정도 마찬가지다. 미스 때 사용자가 7초를 무는 구조는
  그대로 남는다.

## 4. 시장 전체 지표는 요청 시점에 계산하지 않는다

`getMarketTemp()`가 느린 근본 이유는 GAS라서가 아니라 **요청을 받고 나서 전종목 시세를
긁어 점수를 매기기 때문**이다. 증시온도·시가총액 버블은 방문자마다 달라지지 않는
**시장 전체 지표**다. 요청 경로에서 계산할 이유가 없다.

VM으로 옮길 때는 그대로 옮기지 말고 **백그라운드 주기 계산 + 즉시 서빙**으로 바꾼다.
이 저장소에 이미 같은 패턴이 있다:
- 스캔 타이머(`daily_scan` 16:00, `strategy_scan` 16:20, `angle_momentum_scan` 16:25 …)
- 상시 폴러(`foreign_futures.start_background()`, `domestic_futures`, `bond_yield` …)

그러면 방문자 응답은 캐시 읽기 수준(수십 ms)이 되고, 워머 같은 보조 장치도 필요 없다.

## 5. 순서

이득이 큰 것부터, 위험이 낮은 것부터 섞어서 간다.

| 단계 | 대상 | 성격 | 위험 |
|---|---|---|---|
| 1 | `marketTemp=1` → VM `/market-temp` (백그라운드 계산) | 홈 7.1초 제거 | **중** - 배점 이식이 값을 바꾸면 안 됨 |
| 2 | `bubble=1` → VM `/marketcap-bubble` | 홈 3.7초 제거 | 중 |
| 3 | `action=fundamentals` → 이미 있는 `/fundamentals/{code}` | 홉 제거만 | 낮 |
| 4 | `action=foreignFlow` → 이미 있는 `/foreign-flow/{code}` | 중복 제거 | 낮 |
| 5 | 나머지 액션(AI 해설 계열 등) 개별 판단 | | |

각 단계 공통 규칙:
1. VM에 새 경로를 만들고 **프론트는 아직 바꾸지 않는다.**
2. 같은 시각 GAS 응답과 VM 응답을 **컴포넌트별로 대조**한다. 목적은 "숫자가 같아야
   한다"가 아니라 **"설명되지 않는 차이가 없어야 한다"** 이다(2026-09-02 개정). 차이가
   나오면 어느 쪽이 옳은지 규명하고 근거를 문서에 남긴 뒤 결정한다 - GAS를 정답으로
   전제하면 GAS의 결함까지 이식하게 된다(실제로 1단계에서 그런 항목이 둘 나왔다.
   아래 7절 표 참고).
3. 일치가 확인된 뒤 프론트를 VM으로 돌린다.
4. 한 사이클 운영해 문제없으면 **GAS에서 해당 액션을 지운다**(안 지우면 중복이 남아
   다음 사람이 어느 쪽이 진짜인지 모른다 - 지금 `foreignFlow`가 그 상태다).

## 6. GAS를 계속 둘 수도 있는 것

전부 옮기는 게 목적이 아니다. 아래는 남겨도 되는 후보다. 옮길 이유가 생기기 전까지 둔다.
- Google 계정·시트에 붙어야만 하는 작업
- 호출 빈도가 낮고 첫 화면 경로가 아닌 AI 해설류(`marketTempBriefing`, `priceReason` 등)
  단, 이들이 내부적으로 `getMarketTemp()` 같은 무거운 함수를 다시 부르는지는 확인할 것.

## 7. 지금 상태

**1단계 완료(2026-09-02). 프론트가 VM `/market-temp`를 직접 부른다.**

### 종단 비교 결과 — 2026-09-02 13:18~13:19 UTC 실측

| 컴포넌트 | GAS | VM | 판정 |
|---|---|---|---|
| flow(수급) | 13점 (외국인 76 / 기관 39) | 4점 (외국인 27 / 기관 0) | **VM 채택** |
| tradingValue | 7점 (avg5 18.77조) | 4점 (avg5 21.09조) | **VM 채택** |
| avgChange | 0점 (-3.74%) | 0점 (-2.98%) | 점수 동일 |
| riseRatio | 0점 (14/220) | 0점 (17/217) | 점수 동일 |
| sectorStrength | 0점 (1/74) | 0점 | 점수 동일 |

값이 같은 정의를 쓰는 항목(`tradingValue.today` GAS 17.107조 / VM 17.256조)은 수집 시점
차이 수준에서 일치했다. **갈린 두 항목은 모두 입력 소스가 다른 것들이고, 둘 다 GAS 쪽에
결함이 있어 VM을 정답으로 확정했다.**

**flow** — GAS는 KODEX 200(069500) ETF를 시장 수급 대리지표로 썼다. VM에서 같은 ETF를 KIS
종목별투자자매매동향으로 받아보니 64일 중 63일이 0이었다. 기준선(20일 |순매매| 평균×5)이
무너져 비율이 항상 ±1.0으로 포화된다. VM은 `investor_trend_daily`의 코스피 **시장 전체**
수급으로 교체했고 배점 공식은 그대로 뒀다(사용자 승인, 2026-09-01).

**tradingValue** — GAS `computeVolumeScore_`는 5일 이력을 `PropertiesService`에 쌓는데,
①방문이 있어야만 기록되고 ②그날 마지막 호출이 장중이면 장중 누적값이 그 날짜 값으로
굳으며 ③거래일이 아니라 호출된 날짜 기준이라 휴장일이 섞일 수 있다. VM은 `daily_prices`
에서 실제 거래일의 종가×거래량을 SQL로 뽑는다. GAS avg5가 12% 낮은 것이 이 과소평가와
방향·크기 모두 들어맞는다.

**결과**: 화면 온도가 낮아진다(전환 직전 실측 기준 45.7점 → 30점대). 값이 바뀌는 게 아니라
**그동안 과대평가돼 있던 것이 교정되는 것**이다.

### 이전 진행 기록

- `market_temp_score.py` (배점) — 완료. GAS 응답 fixture와 12건 일치(컴포넌트 점수·band·
  최종 score 72/max 120/temp 24.0).
- `market_temp_data.py` (수집) — 시세 유래 4개 컴포넌트 완료. 라이브 네이버 API로
  238종목 중 236개 수신, GAS 골든과 **네 컴포넌트 모두 같은 점수**
  (tradingValue 11, avgChange 4, riseRatio 3, sectorStrength 4). 원시값은 측정 시점이
  달라 다르지만 밴드가 같다.
- `market_temp_data.py` 나머지 입력 — 완료. VIX·원달러·S&P500선물은 `future_prices`에서
  읽고(폴러가 이미 수집), week52는 캐시 파일 직접 읽기, 수급 변환은 GAS 골든과 일치
  (외국인 61/기관 72/최종 13).
- `market_temp.py` + `/market-temp` 엔드포인트 — 완료. **백그라운드 3분 주기 계산**,
  방문자는 저장된 값만 읽는다(로컬 실측 캐시 읽기 0.0ms). 기동 직후 1회만 요청 경로에서
  계산한다. 일별 온도 이력은 **운영 DB 안 `market_temp_daily` 테이블**에 넣었다 -
  DB 파일을 6번째로 늘리지 않기 위해서다(2026-08-31 유지보수 점검에서 5개 중 2개만
  관리되던 걸 고친 직후라 같은 함정을 다시 파지 않는다).
  종단 실행 확인: 실데이터가 있는 컴포넌트(vix 16, avgChange 4, riseRatio 3,
  sectorStrength 4)가 GAS와 일치.

**남은 것(프론트 전환 전 필수)**
1. **수급 배선** — 지금 중립(50/50) 고정. KODEX200 일별 외국인/기관 순매매를
   `kiwoom_market`/`foreign_flow_compute`에서 가져와 `flow_ratio_from_daily`에 넣어야 한다.
2. **`scoreKofiaCredit_` 이식**(GAS 96줄) — 지금은 항상 '검증 중'이라 총점에서 빠지고
   만점이 120 → 110이 된다. 온도 정규화는 맞지만 GAS와 총점이 달라 비교가 안 된다.
3. **VM 배포 후 GAS와 종단 비교** — 같은 시각에 두 응답을 받아 컴포넌트별로 대조.

- ~~프론트는 아직 GAS를 쓴다~~ → **2026-09-02 전환 완료.** 수급 배선과 `scoreKofiaCredit_`
  이식은 그 사이 끝났고(라이브 응답에서 `flow.combined100`·`creditRisk.available: true`
  확인), 종단 비교는 위 표로 마무리했다.

**남은 것**
- 한 사이클(최소 하루) 운영해 문제가 없으면 GAS `marketTemp` 액션을 제거한다(5절 규칙 4).
  지금은 남겨두되 아무도 호출하지 않는다 - 폴백으로 쓰지 않는다. VM이 죽었을 때 GAS
  숫자로 조용히 갈아타면 온도 기준이 말없이 바뀌어 오히려 나쁘다.
- `?marketTempBriefing=1`(AI 브리핑)은 아직 GAS다. 내부에서 `getMarketTemp()`를 다시
  부르므로, `marketTemp` 액션을 지울 때 이쪽 의존을 먼저 끊어야 한다.

### 배선하며 결정해야 할 것
1. ~~**거래대금 5일 이력**~~ — **해결됨(결정 불필요)**. GAS가 `PropertiesService`에
   직접 쌓아둬서 이관이 필요한 줄 알았으나, `daily_scan.py`가 이미 **KRX 전종목 일봉을
   `daily_prices`에 넣고 있어** 같은 값을 SQL로 재구성할 수 있다
   (`market_temp_data.prior_trading_values`). 이관도 중립 기간도 없다. 오히려 GAS는
   프로퍼티가 초기화되면 이력을 잃는 구조라 이쪽이 더 튼튼하다.
2. **VIX/S&P500 출처 변경**: GAS는 Yahoo(`^VIX`, `ES=F`), VM 폴러는 네이버(`.VIX`,
   `EScv1`). 같은 지수라 값은 거의 같지만 배점 경계(VIX 15/20/25/30)를 스칠 수 있다.
   외부 호출을 늘리지 않으려고 기존 폴러를 쓰기로 했고, 종단 비교에서 점수가 갈리면
   경계값 때문인지 먼저 확인한다.
3. **NXT 시간외 단일가**: GAS `applyNxtOverride_`는 장 종료 후 시간외 가격을 우선한다.
   증시온도는 정규장 기준 지표라 VM 이식본은 적용하지 않았다(장중에는 동일 동작).
   장 종료 후 값이 갈리면 이 차이다.

## 8. 참고 - 첫 페인트(FCP)는 손댈 게 남아 있지 않다

홈 렌더 블로킹 21개 중 **16개가 티스토리 소유**(kakao.min.js·tiara·jquery 등, 마지막
659ms)이고 우리 자산은 3개로 436ms에 이미 끝난다. 우리 CSS/JS를 더 줄여도 첫 페인트는
안 내려간다. 남은 개선 여지는 전부 **데이터 도착 시각**이고, 그게 이 문서의 대상이다.
