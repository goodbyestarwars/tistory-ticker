# tistory-ticker

티스토리 블로그(ghlee.tistory.com, 9bolt 스킨)의 티커 툴팁·섹터 대시보드·종목뉴스·마켓리본 소스.
**GitHub Pages로 서빙됨**: `https://goodbyestarwars.github.io/tistory-ticker/{경로}` — master에 push하면 1~10분 내 블로그에 자동 반영된다(cache max-age=600). 파일명 버저닝 금지: 같은 파일을 계속 갱신할 것(URL이 블로그 HTML에 박혀 있음).

**2026-07-27부터 "9Pay 증권" 개편 진행 중**: 전통적인 블로그 레이아웃을 네이버페이 증권 스타일 증권 플랫폼 UI로 바꾸는 대규모 작업(전체 레이아웃/메뉴 위치/홈 개편/증시검색 신규/호가창 병합/차트 기능/종목분석 개선/종목뉴스 개선/MY 메뉴 신설/반응형, 이후 스크린샷 기반 반복 수정 라운드 진행 중). 왼쪽 세로 사이드바 → navbar 바로 아래 가로 상단 메뉴바(`js/skin-menu.js` 행 참고), 종목검색이 navbar에 상주(`js/stock-search-panel.js`), 증시검색/호가창/차트 통합 페이지(`js/stock-search.js`) 신설, MY(관심종목, `js/watchlist.js`) 신설이 핵심. **작업 브랜치는 항상 최종적으로 master에 fast-forward merge까지 해야 실제 배포됨**(session 브랜치에만 push하면 GitHub Pages에 반영 안 됨 - `git checkout master && git pull && git merge --ff-only <branch> && git push origin master`).

## 파일 구성

| 경로 | 역할 |
|---|---|
| `data/sectors-v3.js` | 섹터 대시보드 데이터. `window.SECTOR_MAP = { "섹터명": [{ name, code, market }, ...] }` (market: "KOSPI"/"KOSDAQ") |
| `js/sector-dashboard-v4.js` | 섹터 대시보드 렌더링(시장 뱃지 P/Q 포함, 뱃지 CSS는 JS가 주입) |
| `css/sector-dashboard-v3.css` | 대시보드 스타일(카드/히트맵/다크모드) |
| `data/krx_map.js` | 종목명→코드 전 종목 매핑(티커 툴팁·종목뉴스용) |
| `js/ticker-tooltip-v5.js`(+CSS는 `css/ticker-tooltip-v3.css` - JS만 v5로 올라갔고 CSS 파일명은 v3에 머물러 있음, 버저닝 파일명 규칙상 새 파일 만들지 않고 계속 이 이름 사용), `js/stock-news.js`, `js/market-ribbon.js` | 본문 툴팁 / 종목뉴스 / 상단 리본(`market-ribbon`은 2026-07-16부로 폐기, `js/quick-indices.js`로 기능 이관·display:none). **종목뉴스는 2026-07-27 "9Pay 증권" 개편으로 큰 사진 카드 1개+헤드라인 리스트(상단)+이미지 카드 그리드(하단) 레이아웃으로 전면 재구성됨**(네이버 증권 "뉴스·리서치" 탭 참고, GAS 응답 필드는 그대로). **2026-07-28**: `item.image`가 없을 때 회색 빈 썸네일 박스(`sn-news-thumb-empty`)를 그리던 걸 없애고 텍스트만 표시하도록 변경(사용자 요청, "위젯 재개편 예정이라 일단 임시조치"라고 밝힘 - 다음에 이 위젯을 다시 손볼 때 그 개편이 아직 안 왔다면 이 텍스트-only 처리를 유지할 것). **같은 날 다크모드 버그 수정**: 본문 툴팁(`.ticker-tooltip`)·"이 글에서 언급된 종목" 요약 박스(`.ticker-mentioned-box`)·인라인 티커 배지(`.ticker-badge`)가 전부 `@media (prefers-color-scheme: dark)`로 다크모드를 판정하고 있었음 - 이 스킨의 다크모드는 토글 스위치가 `html.dark` 클래스를 켜는 방식이라 OS 설정과 무관하게 켜지는데, media query라 토글과 완전히 따로 놀았던 게 원인. 게다가 `.ticker-mentioned-box`/`.ticker-badge`는 본문(`.post-single-body`) 안에 렌더링돼 사이트 공통 블랑켓 규칙(`style.css`의 `html.dark .post-single-body *{color:#eaeef5 !important}`)에 그대로 노출되는데 이 위젯만 `!important` 대응이 없어서, 다크모드에서 박스 배경은 라이트 그대로(흰색)인데 글자만 흰색으로 덮어써져 "이 글에서 언급된 종목" 타이틀이 흰 바탕에 흰 글자로 실제로 안 보이는 버그였음(사용자 실측 리포트) - `html.dark #컨테이너ID ... !important` 패턴(CLAUDE.md 기타 규칙)으로 전면 교체하고, 배색은 `js/foreign-flow.js` 매물대 위젯과 동일한 "크림 카드"(`#F5EFE0` 배경 + `#1A1A1A` 테두리/글자) 조합을 재사용해 사이트 전체 다크모드 톤을 통일. 인라인 배지는 카드 위(밝은 배경)냐 본문 텍스트 사이(어두운 배경)냐에 따라 다른 색 세트를 쓴다(선택자 특이성으로 분기 - `.ticker-mentioned-box .ticker-badge`가 일반 `.ticker-badge`보다 우선). **종목뉴스 2026-07-28**: ①오늘 발행분만 표시(`item.datetime`의 `YYYYMMDDHHmm` 앞 8자리를 로컬 오늘 날짜와 비교 필터, 사용자 요청 "오늘 뉴스만") ②큰 사진 카드(featured)에만 있던 본문 스니펫(`sn-news-snippet`)을 제거해 헤드라인/그리드 카드와 동일하게 사진+제목+출처/시각만 표시하도록 통일(사용자 요청 "작은 미리보기+제목만 있어도 충분"). |
| `js/quick-indices.js`, `css/quick-indices.css` | **관심지수 리본** - navbar 바로 아래 모든 페이지 공통 바(2026-07-17 13차부터 position:absolute - 스크롤하면 페이지와 함께 올라가 사라짐). 코스피/코스닥/코스피 야간선물/금 선물/BTC 등은 VM(`https://goodbyestar.cloud/futures`, 이력 있어 미니차트 가능 - BTC는 2026-07-17 14차부터 `scripts/cloud-vm/btc_futures.py`가 업비트를 서버사이드로 수집, 클라이언트 직접 호출 방식은 방문자별 레이트리밋/CORS로 불안정해 폐기), 원달러는 GAS `?market=1`. "큰 카드 1개 + 그리드(3줄, 168px 열이 오른쪽으로 auto-flow)" 밀집 배치(토스증권 참고) - 최대 11종이 전부 한 화면에 들어가 페이징 없음(2026-07-17 10차에 화살표 페이징 제거), 남는 폭은 긴급속보 패널이 차지. 접기/펼치기·표시 종목 커스텀은 localStorage(`qi_selected_v1`/`qi_collapsed_v1`). 자기 높이를 `--qi-height`(style.css `:root`)에 실시간 반영해서 `.page-wrap`/`.sidebar-left`/`.sidebar-right`의 좌표 오프셋이 항상 맞물리게 함 - 이 바의 높이를 바꾸면 style.css의 `calc(...+var(--qi-height))` 오프셋도 같이 확인할 것. **2026-07-17(9차)부터 KRX 공시 티커(옛 별도 고정 바)를 이 리본 오른쪽 "긴급속보"(`.qi-news`) 패널로 흡수** - fetch/파싱 로직은 같은 GAS(`?market=0`)를 그대로 쓰되 가로 스크롤 대신 세로 스크롤 목록으로 렌더링. 장외 시간엔 공시 RSS가 비므로 GAS `?rankNews=1`(네이버 뉴스 시황 헤드라인)로 폴백. **2026-07-28: 홈("/")에서만 노출하도록 변경**(사용자 요청 "홈 버튼 이외에선 위젯 제거해줘") - `init()`이 `location.pathname`이 `/`가 아니면 mount/fetch를 아예 안 하고 조기 리턴(`isHomePage()`). 다른 페이지에서 `--qi-height`가 기본값(140px)으로 남아 리본 없는 빈 여백이 생기는 문제가 있었는데, style.css의 `html.full-width-page{--qi-height:0}` CSS 오버라이드만으로는 라이브에서 실제로 안 먹혀서(하드리프레시 후에도 재현 - skin.html에 git 밖의 숨은 CSS가 이 계산을 덮어쓰는 전례가 있음, 위 2026-07-16 참고) **init()이 조기 리턴할 때 JS로 `documentElement.style.setProperty('--qi-height','0px')`를 직접 박아 넣어 어떤 스타일시트보다 확실히 이기도록 이중 방어**함. |
| `js/overnight-market.js`, `css/overnight-market.css` | **글로벌 시장지표** 페이지(`/pages/overnight-market` - 표시 이름은 "전체 종합지수"→"보조지수"→"글로벌 시장지표" 순으로 여러 차례 개칭됐지만 파일명/URL은 티스토리 HTML에 박혀 있어 유지). 코스피/코스닥, 미국 3대 지수(현물+선물)·SOX, VIX, WTI·금선물, 원/달러 환율, 국채금리(국고채3년/미국10·2·30년), BTC·ETH를 카테고리별로 묶어 카드로 표시 - 카테고리마다 `direction`(1=상승호재/-1=상승악재/0=해석없음)을 매겨 종합 요약(`buildSummaryText`)과 미니차트 참고선 표시에 반영한다(환율·VIX·채권은 "오르면 좋다"로 뭉뚱그리지 않음). 데이터는 전부 VM `goodbyestar.cloud/futures`(상시 수집) 하나로 통일, 미니차트는 TradingView Lightweight Charts(CDN 지연 로드)로 그린 베이스라인 스파크라인 - 기준선(전일 종가, 회색 점선) 위에 WTI/VIX/환율/채권/BTC/ETH는 "최근 12개월 평균" 참고선(주황 실선, VM `/futures/avg?days=`)이 추가로 얹힌다. **2026-07-21: BTC/ETH 카드에 6개월 평균선(보라 점선) 추가** - "비트코인이 6개월 선행한다"는 사용자 질문에 팩트체크한 결과 통계적으로 검증된 관계는 아니라고 판단, 위젯엔 중립적인 이동평균선 해석 문구만 표기(선행지표 주장은 넣지 않음). 같은 날 **글로벌 시장지표 요약 박스**(`renderSummary`)와 **참고의견 박스**(Groq AI 해설, `renderAiSummary`)를 SVG 아이콘 + 카테고리/문장별 줄바꿈 + 검은색 텍스트로 개편(예전엔 " · "로 이어붙인 한 줄 blob + 톤별 색상 코딩이었음). AI 해설은 GAS `?action=subIndexAnalysis`(`gas/ticker-proxy.gs`의 `getSubIndexAnalysis`, Groq `llama-3.3-70b-versatile`)가 만들고, 프론트는 `splitSentences()`로 문장 종결부호 기준 잘라 문장마다 자기 줄에 그린다 - **문장 분리 정규식은 숫자 안의 소수점(예: "26,107.01")을 문장 끝으로 오인하지 않도록 `(?:[^.!?]|\.(?=\d))+[.!?]+(?!\d)` 패턴을 반드시 유지할 것**(GAS의 검증 로직과 동일 정규식 공유 - 한쪽만 고치면 다시 어긋남). AI 해설 프롬프트는 "지표 이름 없이 등락률 숫자만 있는 문장 금지"를 여러 차례 강화했음(1차: 이름+소수점 규칙 명시 → 2차: 서술형 문체 요구 → 3차: 문장을 정확히 1개로 강제 + temperature 0.2 → 4차: 검증을 "숫자(%) 있는 문장엔 지표명 필수"로 일반화) - 사용자가 라이브에서 이상한 응답("59% 하락했다" 등 지표명 없는 단답형, 실제로는 +0.59%의 소수점이 빠진 것으로 추정)을 볼 때마다 실측 사례를 그대로 프롬프트의 "나쁜 예"에 추가해온 이력이 있으니, 다음에 또 비슷한 문제가 보이면 `isSubIndexAnalysisValid_`(응답에 지표명이 없으면 캐시하지 않고 박스를 숨김)부터 확인할 것. GAS 쪽 캐시 키(`sub_index_analysis_v7`)는 프롬프트/검증 로직을 바꿀 때마다 버전을 올려야 기존 캐시가 즉시 무효화됨. |
| `data/marketcap-codes.js`, `js/marketcap-bubble.js`, `css/marketcap-bubble.css` | 시가총액 히트맵(트리맵). ETF10/INVERSE4/단일종목레버리지 합산 2종은 `data/marketcap-codes.js`, 코스피·코스닥 개별종목은 `data/sectors-v3.js` 전체 풀(업종 태그 포함, 업종 필터 지원)을 GAS가 재사용. 등락률 -3%~+3% 7단계 색상. GAS `?bubble=1` 액션을 45초 간격 폴링 |
| `gas/ticker-proxy.gs` | GAS 프록시 소스(시세·뉴스·AI요약·증시온도·히트맵·수급·공매도압박·연기금·랭킹뉴스·차트패턴스캔·오늘의투자시그널). 수정 시 script.google.com에서 수동 재배포 필요 — push만으로는 반영 안 됨. 히트맵 ETF/INVERSE/레버리지 구성 변경 시 `data/marketcap-codes.js`와 이 파일의 `MARKETCAP_CODES` 둘 다 수정. **`scanInvestSignal`(하루 1회 트리거)은 `data/investor-flow-cache.js`를 HTTP로 재fetch해서 재사용** — 이 파일 스키마(short/pension 필드명)가 바뀌면 GAS의 파싱 로직도 같이 고쳐야 함. **랭킹뉴스(`?rankNews=1`)는 2026-07-18부터 네이버를 직접 안 부르고 VM `/naver-news`를 거침**(`scripts/cloud-vm/naver_news.py`) — 네이버 검색API가 NCP API HUB로 이관되며 IP 화이트리스트(최대 10개)를 지원하게 됐는데, GAS(UrlFetchApp)는 고정 IP가 없어 화이트리스트를 못 걸어서 고정 IP를 가진 VM이 대신 호출하도록 우회함. VM엔 `NAVER_APIHUB_CLIENT_ID`/`NAVER_APIHUB_CLIENT_SECRET` 환경변수 필요(NCP API HUB 콘솔에서 Search API 신청 시 발급 — 계정 전체 IAM 키(`ncp_iam_*`)와는 다른 별개 키) |
| `js/foreign-flow.js`, `css/foreign-flow.css` | 종목별 **개인·외국인·기관** 수급 위젯(연속매매·추세전환 뱃지 포함). 데이터 소스 우선순위: ①VM `goodbyestar.cloud/foreign-flow/{code}`(온디맨드, 인증없음, 5분 메모리 캐시) → 실패 시 ②GAS `?action=foreignFlow`(네이버 frgn.naver 크롤링, 개인 없음, 최종 폴백). VM 내부 로직(`scripts/cloud-vm/kiwoom_market.py`의 `fetch_foreign_inst_daily`)은 **2026-07-19부터 KIS(한국투자증권) API가 1차 소스**: 종목별투자자매매동향(일별), `FID_COND_MRKT_DIV_CODE=UN`(KRX+NXT 통합)로 조회해야 종가·거래량·개인·기관·외국인(`frgn_reg_ntby_qty`, 등록 외국인 기준)이 Toss/키움HTS와 정확히 일치함(실측 확인) — 키움 ka10045/ka10059는 이 두 TR에 거래소구분(stex_tp) 파라미터가 아예 없어서 NXT 체결분이 빠진 축소 거래량만 나오는 구조적 한계가 있음(파라미터로 해소 불가, 상세는 코드 주석). `KIS_APPKEY`/`KIS_APPSECRET` 미설정이거나 KIS 호출 실패 시 예전 키움 ka10045 경로로 자동 폴백. 외국인 보유주수/비중은 KIS에 없는 필드라 키움 ka10008을 소스 불문 항상 사용. '오늘' 장중 실시간 값은 ka10059(유일하게 장중 누적치를 실시간 제공)로 오버레이. **2026-07-11부터 공매도/대차거래/연기금 섹션이 여기 병합됨**(`buildExtraSections`) — `data/investor-flow-cache.js`에 있는 종목만 추가로 표시, 없으면 안내 문구만 노출. 가격 차트(지지/저항+이평선, 최근 1년)는 TradingView Lightweight Charts(오픈소스, CDN 지연 로드)로 렌더링 — 직접 그리는 SVG 아님. **2026-07-20부터 옛 "오늘의 투자시그널" 페이지가 이 페이지 최상단으로 통합됨**(작업지시서 기준, 3차 개편까지 완료) — ①시그널 배너(선택 종목의 종합점수+별점, 연한 파랑 고정색 `#E6F1FB`/`#B5D4F4`, 등급은 문자 대신 "양호/중립/부진" 등 한글 텍스트 칩) ②등급 카운트칩(적극매수~매도 5단계, 클릭 시 아래 리스트가 그 등급으로 필터링) ③2열 레이아웃: 좌측 종목 리스트(필터 없으면 등급 버킷을 적극매수→매도 순으로 이어붙인 기본 리스트, TOP10·더보기→20) + 우측 요약 패널(클릭한 종목의 수급/차트/펀더멘탈/투자의견 요약, "상세 보기" 버튼으로 ⑤ 기존 검색 흐름 연결) ④검색창 폭 50%로 축소. **가중치 탭(수급시그널40%/외국인·기관25%/기술적20%/공매도10%/연기금5%/펀더멘탈) UI는 사용자 확인 후 제거됨**(단일 리스트로 대체, git history 참고). ⑤ 기존 상세 영역(buildSummaryBox)엔 7개 컴포넌트 점수(단기 수급강도/외국인·기관/기술적 점수/공매도 압박/연기금/반대매매/펀더멘탈)를 N점+별점으로 표시(옛 "오늘의 수급"은 "단기 수급강도"로 개명). 데이터 소스는 아래 `js/invest-signal.js` 행 참고. **2026-07-22 차트 개편**: 캔들 오버레이는 이동평균(5/20/60/224)+지지/저항만 남기고 볼린저밴드·일목균형표 선은 뺌(계산 자체는 유지, 종합점수엔 그대로 반영). RSI(14)는 참고행으로 넣었다가 원복 요청으로 다시 별도 미니차트(SVG)로 분리 — 70 이상/30 이하 구간을 각각 빨강/파랑(#d24f45/#1261c4, 사이트 공통 등락색)으로 선형보간 폴리곤 채움(`buildRsiThresholdFill`). 기술적 점수(100점)는 이동평균25+지지15+저항15+일목균형표30+**거래량15**(⑤ 신설)로 재배분 — 거래량은 "많으면 고득점"이 아니라 가격방향과 묶어 채점(급증+상승=15점 강한확인, 급증+하락=0점 분산경고, `computeVolumeScore`). 이 배점·공식은 **`scripts/cloud-vm/pattern_detect.py`의 `compute_tech_score`/`compute_volume_score`와 완전히 동일하게 유지할 것**(daily_scan 배치·종목분석 등급이 어긋나면 안 됨 — `invest_signal.py`와 같은 이유). 일목균형표(구름)는 기본 꺼짐 체크박스로 토글 가능(선행스팬1·2만 하늘색 `#4dabf7` 선+음영) — 구름 음영은 Lightweight Charts의 Series Primitives(v4.1+ 지원, 현재 CDN 4.2.0에 이미 포함)로 실제 채움. **예전엔 "v4가 두 시리즈 사이 채우기를 지원 안 해서 v5 업그레이드가 필요하다"는 TODO가 있었는데 이는 틀린 전제였음** — primitive API가 이미 있는 버전이라 사이트 전체 5개 위젯(foreign-flow/pattern-scan/kospi-futures/overnight-market/quick-indices)이 공유하는 CDN을 올릴 필요 없음. 224일선은 검은색+굵게(라이트) — 다크모드 차트 배경(#222)에서 순검은색이 안 보여 다크모드에선 흰색으로 자동 전환(`ma224Color()`). **2026-07-23**: 사용자가 신일전기(062040)에서 외국인 5일 누적 순매수(+51,876주)인데 당일 하루 반대매매(-4,952주) 하나 때문에 상단 톤이 즉시 "중립"("외국인·기관 수급 방향이 뚜렷하지 않습니다")으로 표시되는 문제를 리포트 — `streak.direction`(최신일부터 역순 연속 방향)만 보고 판정하던 게 원인이었음. `flowDirection(kind, data)` 헬퍼를 도입해 streak이 3일 미만이면 노이즈로 보고 5일 합산(`rolling['5d']`) 부호로 스무딩하고, 3일 이상이면 raw streak을 그대로 신뢰하도록 `flowInterpText`/`flowTone`을 통일. 같은 대화에서 "차트를 풀로 쓰고 싶다"는 요청으로 **매물대(Volume Profile) 오버레이 신규 추가** — 일목균형표(구름) 체크박스 옆에 "매물대 표시" 체크박스, 최근 120거래일(`computeSupportResistance_`와 같은 창) 일봉 고가~저가 구간에 그날 거래량을 분산시켜 24개 가격구간으로 합산하는 근사치(체결가 기준 진짜 매물대 아님, 범례에 명시)를 일목균형표 구름과 동일한 Series Primitive 패턴(캔들 뒤에 렌더링)으로 구현, 거래량 최다 구간(POC)은 강조색 표시. 배포 후 사용자 피드백으로 ①불필요했던 선행스팬1/2 텍스트 범례 제거(체크박스 클릭만으로 충분) ②매물대 막대 최대폭 26%→16%·색상 더 옅게(일반 alpha 0.30→0.16, POC 0.55→0.32) 조정 완료. **참고(버그 아님)**: 같은 시점에 VM(`goodbyestar.cloud`)의 키움 API 공용 토큰 계층(`scripts/cloud-vm/kiwoom_client.py`)이 실패해 가격차트·거래량 TOP5·상한가(서로 무관해 보이는 세 기능이 전부 `kiwoom_client.get_token()`을 공유)가 전종목 동시에 "일봉 데이터를 가져오지 못했습니다"로 죽는 장애가 있었음 — `sudo systemctl restart kiwoom-api`(+ `sudo journalctl -u kiwoom-api -n 200`으로 401/429/토큰 발급 실패 로그 확인)로 복구, 근본 원인(앱키 만료 등)은 미확인. **2026-07-27 "매물대 아파트" 위젯 추가**(`buildAptBuildingHtml` 등) - 네이버페이증권 "다른 투자자들은 얼마에 샀어요"(계좌 연동 자기신고 데이터) 참고했지만 우리는 그 데이터가 없어 `computeAptData`로 근사(최근 120거래일 일별 고가~저가 구간에 그날 물량을 분산해 24개 가격 bin으로 합산, 전체/개인/외국인/기관 4탭 - bin 경계는 네 탭이 공유해 층 비교 가능). 건물 실루엣(위로 갈수록 좁아짐)+층별 창문 7개(불켜진 개수=물량 비중)+옥상 전망대/안테나/경광등+로비(현관)+지하실+헬기/사다리 타는 사람/나무 등은 전부 장식(데이터 무관). **2026-07-28 색상 변경**: 꺼진 창 남색→`#141414`, 켜진 창 노란색→`#2BEE34`, 건물(벽/옥상/로비) 회색→크림색 `#F5EFE0`, 외곽선 `#1A1A1A`(라이트/다크모드 동일 팔레트). **같은 날 사용자가 손그림 스케치(흰 종이+검은 잉크선 건물 일러스트) 이미지를 보여주며 "이런 이미지로 안되나" 요청 → 색 채운 블록을 투명배경+외곽선만 있는 "선 그림" 스타일로 재구성했다가 배포 후 "설마 니가 그린게 이거니?"라는 반응으로 즉시 revert함** - 원인은 데이터 밀도: 참고 이미지는 층이 5~6개뿐인 큰 손그림인데 이 위젯은 24개 bin × 창문 7개 = 최대 168개의 얇은 테두리 박스를 눌러 담아야 해서 스타일만 바꿔선 그 느낌이 안 나고 깨진 격자처럼 보였음 - **다음에 "더 예쁘게/일러스트처럼" 요청이 다시 오면 색 팔레트만 바꾸는 시도 대신 먼저 bin 개수를 줄이거나(예: 24→5~6, `APT_LABEL_BANDS` 수준) 층 표현 방식 자체를 바꾸는 걸 검토할 것 - 지금 24-bin 구조를 유지하는 한 촘촘한 데이터 그리드 룩을 벗어나기 어렵다.** **2026-07-27 전면 리디자인("매물대 UI 리디자인 작업지시서")**: 위 교훈대로 팔레트 대신 표현 방식 자체를 바꿈 - 층별 창문(APT_WINDOWS_PER_FLOOR)이 켜지는 아파트 실루엣을 완전히 폐기하고, 가격 레일(중앙)을 기준으로 좌(매도, `#4A90E2`)·우(매수, `#FF5B6E`) 막대가 뻗는 듀얼 바 차트로 교체(`buildAptChartHtml`). **데이터 스키마도 함께 바뀜**: `computeAptData`의 bin이 기존 단일 `qty`에서 `buyQty`/`sellQty`로 분리됨 - 개인/외국인/기관 탭은 그날 실측 순매수(+)/순매도(-) 부호를 그대로 매수/매도 벽으로 나누고(근사 아님, 기존엔 순매도일을 그냥 버렸음), "전체" 탭만 체결 태깅이 없어 그날 등락 방향(`change_pct`)으로 그날 총거래량 전체를 매수/매도 한쪽에 배정하는 근사(OBV류 관례)를 씀 - 각주에 명시. 좌측 구간 안내판(탑층/고층/중층/저층/로비)은 24bin을 5개 구간으로 나눠 구간 첫 행에만 라벨을 표시(rowspan 없이 시각적으로 동일 효과). 현재가 행은 주황 테두리+배경틴트+pulse 애니메이션+우측 "◀현재가" 뱃지로 강조, 매도/매수 각각의 최대 매물대 막대는 glow(box-shadow) 처리. 막대는 `transform:scaleX()` 기반으로 0에서 목표폭까지 자라나는 진입 애니메이션(탭 전환 시에도 재생, `playAptEntrance`). 하단에 매도/매수 우위 구간(연속 bin run 탐지, 1~5단계 강도 dot)·최대 매물대(매도/매수)·총합 통계 카드 4~6종 추가. 옛 헬기/사다리/지하실 등 장식 이스터에그는 "데이터 컨테이너" 원칙에 맞지 않아 전부 제거. **같은 날 Ver.2 요청("매물대 UI 리디자인 작업지시서 Ver.2") - "주가가 사는 아파트" 컨셉을 다시 입혀달라며 옥상 헬기/사다리/로비/지하실을 재요청**: Ver.1의 듀얼 바 데이터 표현(매도/매수 막대)은 그대로 두고 장식만 얹었다 - 옥상에 헬리패드+헬기(로터 회전 애니메이션 `ff-apt-rotor-spin`)+건물 이름표, 층마다 가격 좌우에 미니 창문 아이콘 2개(`ff-apt-mini-window`), 좌측 구간 안내판에 가격범위 부기(`aptBandRangeText`, 실데이터), 현재가 행에 좌우 강조선(`::before`/`::after`)+말풍선 뱃지, 현재가 층에서 지하실로 이어지는 사다리(오르내리는 사람 이모지, `apt.trendUp`로 방향 결정)를 추가. **직전 리디자인 주석에 남긴 교훈("24bin×창문 다수 = 밀도 폭발")을 그대로 지켜 반복 실패를 피함**: 창문은 24행 전부에 붙이되 행당 딱 2개뿐이라 촘촘한 격자가 되지 않고, 진짜 일러스트 밀도(헬기/사다리/로비 소품/지하실 서브룸 라벨)는 반복되지 않는 고정 구간(옥상 1번·로비 1번·지하실 1번)에만 투입 - 지하실은 "아직 매집되지 않은 가격/추가 하락 가능 영역"을 뜻하는 상징적 공간으로 명시, 실제 bin/거래량과 연결하지 않음(작업지시서 원문 그대로). 사다리의 세로 위치·길이는 `APT_ROW_HEIGHT`(=CSS `.ff-apt-row` height와 반드시 일치해야 하는 공유 상수) 기준 픽셀 계산이라 CSS 행 높이를 바꾸면 이 상수도 같이 확인할 것. **2026-07-28 "전체" 탭 매도총합 0 버그 수정**: `computeAptData`의 "전체" 탭이 그날 등락 방향(`chartData.daily[i].change_pct`)으로 매수/매도를 근사 분류했는데, 이 위젯의 실제 데이터 소스(VM `/ohlc`, `kiwoom_market.py`의 `fetch_daily_ohlc`)엔 애초에 `change_pct` 필드가 없어서(`{date,open,high,low,close,volume}`만 반환) `|| 0` 폴백이 항상 걸려 "0 >= 0"으로 매일 매수 쪽에만 쌓이고 매도 총합이 장중/장마감 여부와 무관하게 항상 0으로 고정되는 버그였음(사용자 리포트, "매도총합이 장 끝나고 나오나?"). 사용자 제안대로 개인/외국인/기관 세 탭의 실측 매수/매도 벽을 bin별로 그대로 합산해 "전체"를 구성하도록 교체(근사가 사라지고 코드도 단순해짐, 각주 문구도 갱신). `apt.trendUp`(사다리 애니메이션 방향)도 같은 이유로 `change_pct` 대신 최근 이틀 종가 비교로 변경. **같은 날 추가 요청**: 헬기 사이즈업(15px→28px), 로비 소품 4개(자동문/화분/우편함/안내판)를 큰 문 하나로 단순화하고 그 아래 지하실로 내려가는 맨홀 뚜껑(원형, 순수 CSS로 그린 금속 뚜껑) 추가. **종목분석 헤더 시세 갱신**: 조회 시점에 스냅샷으로 멈춰 있던 문제(예: 실제 -2%인데 화면엔 +5.99% 그대로 - 이 위젯이 검색 시 한 번만 fetch하고 자동 갱신을 전혀 안 했던 게 원인) - 15초 간격으로 헤더 가격/등락률만 가볍게 패치하는 폴링 추가(`startQuotePolling`/`fetchLiveQuote`의 `force` 인자로 캐시 우회, 전체 리렌더는 스크롤/차트 확대상태를 리셋시켜 오히려 방해가 되므로 하지 않음). **차트 다크모드 사각형 버그**: TradingView Lightweight Charts 십자선의 가격/시각 라벨 배경색이 라이브러리 기본값(다크 팔레트와 무관하게 고정)이라 다크모드에서 안 어울리는 회색 네모로 보였음 - `lwcThemeOptions()`에 `crosshair.vertLine/horzLine.labelBackgroundColor`를 테마별로 명시(같은 문제가 `js/stock-search.js`의 동일 함수에도 있어 같이 수정) - `mergeOptions`가 얕은 병합이라 `crosshair` 키를 두 곳에 나눠 쓰면 뒤에 오는 쪽이 통째로 덮어쓰므로 `mode`까지 전부 `lwcThemeOptions()` 한 곳에 모아야 함(주석 참고). **같은 날 매물대 카드를 "수급" 탭에서 독립 탭으로 분리**: 종목분석 상단 탭이 수급/차트/펀더멘탈 3개에서 수급/매물대/차트/펀더멘탈 4개로 늘어남(`buildViewTabs`) - Ver.2로 매물대 카드 자체가 커지면서 수급 탭이 너무 길어진 게 이유. `activeView`에 `'apt'` 추가, 매물대 카드는 `#ffViewFlow`가 아니라 새 `#ffViewApt`(기본 hidden)에 렌더링. hidden 상태에서 진입 애니메이션 클래스가 이미 다 적용돼버려 탭을 처음 열 때 정지 상태로 보이는 문제가 있어, 매물대 탭을 클릭할 때마다 `playAptEntrance`를 다시 호출해 재생되게 함(내부 전체/개인/외국인/기관 서브탭 전환 때와 동일 패턴). |
| `data/investor-flow-cache.js`, `scripts/fetch_investor_flow.py` | 공매도(누적잔고/평균가/거래비중/Days to Cover/압박점수 100점)·대차거래(잔고/증감률)·연기금(연속순매수일수/구간별 순매수/해석) 캐시. KRX가 완전 차단됐지만 **증권사(키움) 정식 REST API는 이 데이터를 계속 제공**해서 이 경로로 우회. GAS가 아니라 **사용자 PC에서 로컬로 스크립트 실행 → git commit/push**하는 구조(증권사 API 앱키가 IP 등록 필요 + 계좌 연동 개인키라 GAS 같은 공개 서버에 못 둠). `KIWOOM_APPKEY`/`KIWOOM_SECRETKEY` 환경변수 필요(하드코딩 금지), 대상 종목은 `data/sectors-v3.js` 풀(238종목, 전체 종목 아님) 재사용. 데이터는 하루 1회 갱신이라 실시간 서버 불필요 — PC를 하루 한 번 켤 때 실행하는 것으로 충분. **미검증**: REST 엔드포인트는 MCP(`mcp__kiwoom__*`)로 확인한 실제 응답 필드 기준으로 작성했지만 실계정으로 직접 테스트 안 됨 — `TEST_CODES`로 소량 검증 후 `--all` 실행할 것 |
| `js/pattern-scan.js`, `css/pattern-scan.css` | 차트 패턴 스캔 위젯(저점상승형/쌍바닥/역헤드앤숄더/박스권하단/눌림목 5종, 지시서 스펙 그대로) - 캔들차트(TradingView Lightweight Charts, CDN 지연 로드) + 패턴선 오버레이. 모든 패턴은 0~100점 채점(70점 이상만 노출, AI 임의판단 없이 수치조건), 점수+원인+한줄해석을 함께 표시. 리스트는 GAS `?patternScan=1`(하루 1회 시간 트리거로 미리 스캔·캐싱, 눌림목은 MA60 필요해 별도 트리거로 분리), 클릭 시 차트는 `?patternChart=1&code=&pattern=`으로 온디맨드 재크롤링. 스캔 대상은 `data/sectors-v3.js`를 GAS가 fetch해서 재사용(별도 종목 리스트 하드코딩 없음). **2026-07-22**: 상세 차트에 일목균형표(구름) 체크박스 추가(기본 꺼짐) - `js/foreign-flow.js`와 동일 계산·색상(선행스팬1·2만 하늘색 `#4dabf7`, 전환선/기준선/후행스팬은 화면에서 뺌)에 Series Primitives로 구름 음영까지 실제로 채움(v5 업그레이드 불필요 — 상세 사유는 `js/foreign-flow.js` 행 참고) |
| `js/invest-signal.js`, `css/invest-signal.css` | **2026-07-20부로 폐기, `/page/foreign-flow`로 리다이렉트만 함**(작업지시서: 투자시그널→종목분석 통합, 별도 페이지 유지 안 함). `/pages/invest-signal`에는 여전히 `<div id="invest-signal">`이 있는 빈 티스토리 페이지가 남아있고 이 스크립트가 그걸 감지해 즉시 이동시킴 - `css/invest-signal.css`는 이제 로드는 되지만 대상 엘리먼트가 안 그려져 사실상 무해(죽은 코드, 롤백 시 참고용). 카운트/랭킹 UI 자체는 `js/foreign-flow.js`로 이전됨. **데이터 소스**: GAS `?investSignal=1`(`getInvestSignalResult()`)이 VM `goodbyestar.cloud/daily-scan-batch`(하루 1회, `scripts/cloud-vm/daily_scan.py`)를 그대로 fetch해 재포장 — 전 종목(krx_map.js, ~2,691종목) 매수~매도 5등급 카운트/버킷 + 랭킹(수급/외국인·기관 합산/기술적/공매도 낮은순/연기금/펀더멘탈 등 TOP20). 점수 공식(수급37%+외국인기관23%+기술적17%+공매도8%+연기금4%+반대매매3%+펀더멘탈8%)은 `scripts/cloud-vm/invest_signal.py`에 있고 `js/foreign-flow.js`의 동일 공식과 완전히 일치해야 함(둘 중 하나만 고치면 등급이 어긋남). VM 코드는 git push 후 5분 내 자동배포되지만 `daily_scan.py` 배치 자체는 하루 1회만 돌아서 랭킹 신규 필드는 다음 배치부터 채워짐. **GAS 쪽(`getInvestSignalResult()`의 랭킹 키 추가/변경)은 스크립트 수정만으로 안 끝나고 script.google.com에서 수동 재배포까지 해야 실제 반영됨.** |
| `js/watchlist.js`, `css/watchlist.css` | **관심종목 카드 위젯("MY" 메뉴)** - 독립 Tistory Page(`/page/watchlist` placeholder). localStorage(`wl_codes_v1`, 최대 50개, 종목명 자동완성으로 추가) - 신규 GAS 엔드포인트 없이 기존 `?codes=` 시세 API 재사용. **2026-07-27**: `add`/`remove`/`has`를 `window.Watchlist`로 공개 API 노출(`js/stock-search.js`의 ⭐ 버튼과 `js/stock-search-panel.js`의 navbar 실시간검색 ★가 이 저장소 형식이 바뀌어도 안 깨지게 이 함수들을 통해서만 접근 - 두 번째는 이 스크립트가 없는 페이지에서도 동작해야 해서 API가 없으면 `wl_codes_v1`을 직접 같은 스키마로 read/write하는 폴백을 자체적으로 갖고 있음). 카드마다 VM `/foreign-flow/{code}?days=5`로 "외국인 매수·기관 매도" 같은 한 줄 수급 요약(`loadFlowSummaries`)과 "차트 보기 →" 버튼(`STOCK_SEARCH_PAGE_URL`+`?code=&name=`로 증시검색 페이지 딥링크) 추가. |
| `js/order-book.js`, `css/order-book.css` | **실시간 호가창**(증권사 HTS 스타일, 2026-07-27 신설) - 독립 Tistory Page(`/page/order-book`, `<div id="order-book">` 임베드, watchlist.js와 동일한 KRX_MAP 자동완성 검색. `js/skin-menu.js` MENU_ITEMS에도 "호가창" 메뉴로 등록됨). 매도/매수 각 10단계 잔량 사다리는 VM `goodbyestar.cloud/order-book/{code}`(`scripts/cloud-vm/order_book.py`, 키움 REST `ka10004`주식호가요청 직접 호출)를 브라우저가 2초 간격으로 폴링(인증 없음, CORS로 블로그 도메인만 허용, 서버쪽도 1.5초 캐시로 동시 조회 시 키움 호출 중복 방지) - 현재가/등락률은 기존에 검증된 GAS 시세 프록시(`?codes=`)를 그대로 재사용(별도 소스, 같은 2초 주기로 병행 갱신). 매도=파랑/매수=빨강은 사이트 공통 등락색(#1261c4/#d24f45)이 증권사 HTS의 "매도=청색/매수=적색" 관례와 우연히 일치해 그대로 재사용. **미검증**: `ka10004` 응답의 매도1~5차선·매수 전체 필드명은 문서에 매도6~10차선까지만 나와 있어(`kiwoom_api.md`가 필드 목록을 자르는 문서 특성, `market_rank.py`와 동일 사유) 같은 명명 규칙(`sel_{N}th_pre_bid/req`, `buy_{N}th_pre_bid/req`)을 확장한 추정치 - 배포 후 VM 로그(`order_book.py`의 "호가 필드를 하나도 못 찾음" 경고)로 실제 필드명을 확인할 것. **같은 날 "3D 산점도로 표현해줘" 요청으로 뷰 토글(호가창/3D 산점도) 추가** - X 가격·Y 경과시간(초)·Z 잔량 축의 `scatter3d`(Plotly.js gl3d 최소 번들, CDN 지연 로드 - `js/foreign-flow.js`의 TradingView Lightweight Charts와 동일한 "필요할 때만 로드" 패턴)로, 2초 폴링 스냅샷을 슬라이딩 윈도우로 누적해 마켓뎁스가 시간에 따라 쌓이는 모습을 보여준다. **곧이어 "현재가가 매도벽을 뚫는 과정·강도를 보고 싶다"는 피드백으로 2차 수정**: X축을 절대가격 대신 "그 틱 시점 현재가 대비 가격차"로 바꿔 X=0이 항상 현재가가 되도록 정규화(매도벽이 X=0에 가까워지거나 Z(잔량)가 줄어드는 걸 보면 벽이 뚫리는 과정을 읽을 수 있음, xaxis zeroline을 굵게 강조해 기준선 표시) + 누적 윈도우를 1분(30틱)→3분(90틱)으로 확장(사용자가 직접 지정) + 마커 크기도 잔량에 비례시켜 벽 강도를 이중 인코딩. 이 과정에서 `Promise.all([fetchOrderBook, fetchQuote])`가 시세(quote) 하나만 실패해도 호가(book)까지 통째로 버려지던 버그를 발견해 같이 고침 - `fetchQuote().catch(()=>null)`로 격리하고, `recordSnapshot`이 시세 실패 시 직전 기준가를 이어받게 해서(`state.lastBase`) X축 기준가가 순간적으로 사라지는 일이 없게 함. **3차 수정(같은 날, "산점도가 내가 원한 게 아니다 - 호가창을 게임처럼 입체적으로, 벽을 뚫는 느낌으로 보고 싶다")**: 점(마커) 대신 각 호가 단계를 바닥(Z=0)에서 잔량 높이까지 솟은 "기둥"으로 그려 도시 스카이라인처럼 보이게 함(Plotly에 3D bar 타입이 없어 `scatter3d` `mode:'lines'`에 세그먼트 사이를 `null`로 끊는 관용적 트릭 사용, `pushBar()`). 여기에 매도벽 돌파를 실제로 감지해서 게임식 피드백을 주는 로직 추가(`findWallCandidate`/`checkWallBreakthrough`) - 다른 9개 매도호가 평균보다 1.8배 이상 쌓인 호가를 "벽"으로 지정해 그 가격을 추적하다가, 잔량이 최초 확인치의 15% 이하로 줄거나 그 가격대가 사다리에서 아예 사라지면(현재가가 완전히 지나감) "돌파"로 판정 - 매도벽 하나만 동시 추적(요청 확인, 2개 이상 동시 추적은 범위 밖). 돌파 시 3D 뷰 위에 애니메이션 토스트("🎉 71,700원 돌파!", 3.5초 후 자동 소멸)와 하단 누적 기록 리스트(최근 8건)로 표시. 현재가 트레이스(청록)도 유지 - X=0 zeroline과 겹쳐서 "여기가 현재가" 기준을 계속 보여줌. **4차 개편(같은 날, "지금 3D 그래프는 시간축 중심이라 호가 전쟁을 표현 못 한다")**: Y축을 경과시간에서 매수/매도 방향(±1, 두 레인)으로 바꿔 시간을 아예 없애고 매 틱 최신 스냅샷 하나만 `Plotly.react`로 다시 그리는 실시간 애니메이션 방식으로 전환(기둥이 그 자리에서 오르내림) - 매도벽(Y=+1 레인)과 매수벽(Y=-1 레인)이 X=0(현재가, 청록 골대 모양 기준선)을 사이에 두고 마주보는 구도. 저항(매도벽 최대 잔량)/지지(매수벽 최대 잔량)를 상단 HUD 막대 2개로 비교 표시(높이 차이=강도 차이를 그대로 시각화, 사용자 요청)하고, "체결강도"를 새로 추가 - 실제 체결(0B 웹소켓) 연동 없이 **추적 중인 매도벽이 직전 틱 대비 얼마나 줄었는지(peakQty 대비 %, `STRENGTH_SCALE=7`로 스케일)만으로 추정하는 근사치**(사용자에게 "지금 데이터로 근사치 계산" vs "실제 체결강도 새로 구축(0B 웹소켓 VM 연동 필요)" 중 근사치 추천안으로 진행함, UI에도 "실제 체결량 아님" 명시). `HISTORY_MAX`는 공간축 누적이 없어져 90→6으로 대폭 축소(체결강도 계산에 필요한 직전 틱 비교용 버퍼만 유지). **배포 후 실측 버그 수정(같은 날)**: 2초 폴링마다 `Plotly.react`로 다시 그리면서 마우스로 돌려놓은 카메라 각도가 매번 기본값으로 리셋되는 문제 발견 - Plotly `uirevision` 옵션으로 먼저 시도했으나 이 gl3d 번들에서는 안 먹히는 걸 실측 확인, `plotEl._fullLayout.scene.camera`(직전에 실제 반영된 카메라 값)를 직접 읽어와 다음 `Plotly.react` 호출의 layout에 명시적으로 다시 넣어주는 방식으로 해결. 같은 피드백에서 "화면이 너무 작다"는 지적으로 3D 뷰일 때만 위젯 폭을 420px→820px로 넓히고(`setViewMode`가 `.ob-wide` 클래스 토글, 호가창 리스트 뷰는 원래 폭 유지) 플롯 높이도 460px→640px로 키움 - `responsive:true`는 컨테이너 폭이 창 크기 변경 없이 CSS로 바뀌는 경우엔 안 따라와서 `Plotly.Plots.resize()`를 전환 직후 명시적으로 호출. **2026-07-27 최종: 3D 산점도 뷰 폐기 결정** - 5차례 개편(시간축 산점도 → 상대가격 정규화 → 입체 기둥+돌파 토스트 → 매수/매도 방향축+HUD → 카메라/크기 수정)에도 사용자가 원하는 그림이 안 나와 이 방향은 중단하기로 함. **코드/UI 제거는 아직 안 함** - `js/order-book.js`/`css/order-book.css`의 "3D 산점도" 토글 버튼과 Plotly 연동 코드가 라이브에 그대로 남아있는 상태(다음에 재개하거나, 반대로 토글 버튼 자체를 숨길지는 별도 확인 필요 - 호가창(리스트) 뷰는 이 폐기와 무관하게 계속 정상 운영). **2026-07-28 사용자 리포트**: 종목이 -10%로 급락 중인데도 "매도벽 돌파!" 토스트가 뜸 - `checkWallBreakthrough`의 감지 자체("추적하던 매도호가가 사다리에서 사라지거나 거의 소진됨")는 진짜 매수세가 벽을 뚫은 경우와, 가격이 급락해 그 가격대가 상위 10호가 창 밖으로 밀려난 경우를 구분 못 함 - 후자인데 "돌파"라고 하면 오해를 줌. 감지 로직은 그대로 두고 캔들 방향(`quote.changeRate`, `isUpCandle()`)에 따라 문구만 분기(양봉=기존 "돌파" 유지, 음봉="지지라인 방어중"/"지지라인 재구축") - 토스트·돌파 기록 로그(`state.milestones`에 `up` 플래그 저장)·진행 중 안내(`updateBreakoutNote`)·체결강도 HUD 문구까지 전부 이 분기 적용. |
| `js/sidebar-rank.js`, `css/sidebar-rank.css` | **우측 사이드바 실시간 랭킹**(2026-07-20 작업지시서 "9bolt 사이드바 리디자인" Phase 1) - 옛 인기글TOP5/해시태그(둘 다 티스토리 서버 치환 태그라 skin.html에서 완전히 제거됨, git 불가)를 거래량 TOP5/상한가/하한가 3섹션으로 교체. 배당주 TOP은 배당수익률 데이터 소스 자체가 없어(DART 배당공시 미연동) Phase 2로 보류(사용자 확인). VM `goodbyestar.cloud/market-rank`(`scripts/cloud-vm/market_rank.py`, 키움 REST 랭킹 TR `ka10030`당일거래량상위/`ka10017`상하한가 직접 호출, 우리가 전종목 스캔 안 함)를 브라우저가 직접 호출(인증 없음, CORS로 블로그 도메인만 허용, 30초 서버캐시) - `/futures`, `/option-flow`와 동일 패턴. **미검증**: 두 TR 다 공식 문서에 응답 배열을 감싸는 최상위 키 이름이 안 나와 있어 `market_rank.py`가 "리스트 타입 필드를 자동으로 찾는" 방어적 파싱을 쓴다 - 정규장 중 배포 후 로그(`option board` 디버깅과 동일한 진단 패턴)로 실제 동작 확인 필요. **2026-07-22**: 원래 거래대금(ka10032) 기준이었으나 단가 비싼 종목(SK하이닉스 등)만 걸리는 문제로 거래량(ka10030) 기준으로 교체 - ka10030의 `sort_tp` 등 파라미터값은 공식 문서 필드 목록만 근거로 채운 추정치라 배포 후 로그로 재확인 필요. 색상은 작업지시서 원문(#E24B4A/#378ADD) 대신 사이트 공통색(#d24f45/#1261c4)을 그대로 씀(불일치 방지) |
| `js/investor-trend-widget.js`, `css/investor-trend-widget.css` | **메인 페이지 "투자자별 매매 동향" 위젯**(작업지시서 #4, 2026-07-20 + UI개선 지시서 2026-07-21) - 코스피/코스닥 시장별 개인/외국인/기관계 순매수(억원)를 일/주/월 탭 + 시장 탭 + 0선 기준 막대차트 + 수치테이블로 표시, 예전 공지사항 고정 카드(`#pinnedNotice`, `js/skin-shell.js` 아님 - skin.html에 인라인으로 있던 fetch 스크립트)를 대체(공지 데이터 자체는 삭제 안 함 - `/notice` 글과 skin.html의 `s_notice_rep` 루프는 그대로 유지, 홈 상단 노출만 내림). **지시서는 백엔드를 키움 TR(`get_investor_summary`/`get_investor_trend`, stk_cd 없이 호출하면 시장 전체 값)로 가정했지만 틀렸음** - 2026-07-20 MCP로 두 TR을 stk_cd 없이 직접 호출해 `오류: 'stk_cd'`로 실패하는 것을 확인, 둘 다 종목별 조회 전용이라 시장 전체 집계를 지원 안 함. **2026-07-20 2차**: `kis-code-assistant-mcp`(한국투자증권 공식 코드검색 MCP, `claude mcp add`로 user scope 등록 — `C:\Users\goodb\.mcp-servers\open-trading-api\MCP\KIS Code Assistant MCP`)로 KIS TR `FHPTJ04040000`(시장별 투자자매매동향(일별), HTS `[0404]` 화면과 1:1 대응)을 찾아 `scripts/cloud-vm/kis_client.py`의 `fetch_market_investor_daily`로 추가, `investor_trend.py`가 이걸 1차 소스로 씀(KIS_APPKEY/APPSECRET 없거나 실패 시 아래 네이버로 자동 폴백). 공식 예제가 날짜1=날짜2(하루치)만 검증돼 있어 날짜별로 반복 호출해 SQLite(`investor_trend_daily`)에 쌓는 방식(1분마다 오늘 하루치, 시작 시 최대 140영업일 백필 — 사용자 판단: "숫자 데이터라 용량 부담 없으니 일별 저장이 낫다"). **`_tr_pbmn`(거래대금) 필드의 원→억원 환산(/1e8)은 미검증** — `kis-code-assistant-mcp`는 코드 검색 전용이라 실호출 테스트를 못 함, 배포 후 네이버 저장값과 반드시 대조할 것. 네이버(`finance.naver.com/sise/investorDealTrendDay.naver`, `bizdate`+`page` 파라미터 페이징, `domestic_futures.py`와 동일한 우회 사유)는 폴백 경로로 유지. VM `goodbyestar.cloud/investor-trend?period=day\|week\|month`를 브라우저가 직접 호출(인증 없음, CORS로 블로그 도메인만 허용) - `/futures`, `/market-rank`와 동일한 패턴, 주/월 집계는 이 엔드포인트가 매 요청마다 SQLite 캐시 위에서 계산. 색상(개인 #AAAAAA/외국인 #d24f45/기관 #378ADD)은 지시서 원문(민트 #1D9E75)에서 사용자 요청으로 사이트 공통 상승색(빨강)으로 변경. **2026-07-20 3차(배포 후 실측)**: `/1e8`이 실제로는 100만배 과다 축소였음 - 라이브 응답을 네이버 캐시값과 대조해 백만원 단위임을 확인, `/100`으로 수정. 외국인 필드도 종목별 KIS TR(FHPTJ04160001)의 "등록외국인만 써야 Toss와 일치" 규칙을 따라 `frgn_ntby_tr_pbmn`(전체)에서 `frgn_reg_ntby_pbmn`(등록만)으로 교체. 이후 사용자가 실측(토스+키움 HTS `[0780]`)한 결과 이 KIS TR 자체가 그 화면 값과 다름을 확인(신선한 데이터끼리 비교, 타이밍 문제 아님) - 대안으로 키움 `ka10051`(업종별투자자순매수)과 KIS `FHPTJ04030000`(시장별 투자자매매동향(시세))도 시도했으나 전부 불일치. **키움 공식 AI에 문의한 결과 `[0780]`과 1:1 대응하는 공개 REST TR은 없고, `ka10059`로 전 종목(800여개)을 순회 합산해야 하는데 그래도 ETF 포함여부·단위처리 차이로 완전 일치는 보장 안 됨**(매 갱신마다 800여회 호출은 비현실적이라 미채택) - 최종적으로 KIS 일별을 그대로 유지. **2026-07-21 정정**: 사용자가 KRX 공식 통계(코스피 기관/외국인/개인 순매수, 십억원 단위)를 직접 대조해보니 KIS 일별 값이 KRX 원본과 거의 정확히 일치함을 확인(개인 오차 0.07억, 기관 4.9억 수준 - 반올림급) - 안 맞았던 건 오히려 키움/토스 HTS `[0780]` 위젯 쪽이 KRX 공식 수치와 다른 자체 집계를 쓰고 있었던 것으로 결론(데이터소스 선택은 처음부터 맞았음). 키움 ka10051은 3순위 폴백으로 유지(`fetch_kiwoom_today`).

**2026-07-21 코스닥 추가 + 일(日) 탭 UI 개편**(작업지시서 `작업지시서_투자자매매동향_UI개선.md` + 사용자 요청 "코스피 코스닥 선물 3개 나눌 수 있어?" 병합 진행): `investor_trend_daily` 테이블을 `(market, date)` 복합키로 마이그레이션(`db_schema._migrate_investor_trend_market`, 기존 데이터는 KOSPI로 자동 태깅 - SQLite는 PK 변경이 ALTER로 안 돼 테이블 재생성 방식). `fetch_market_investor_daily`의 `market_iscd`('KSP'/'KSQ')로 코스피/코스닥 재사용, 코스닥은 네이버 폴백이 없어서(`investorDealTrendDay.naver`가 sosok 파라미터를 바꿔도 코스피 외엔 빈 결과 - 2026-07-21 재확인) KIS 실패 시 키움 `ka10051`(mrkt_tp=1)로 바로 폴백. **코스피 선물은 데이터 소스 자체가 없어 범위에서 제외** - KIS 국내선물옵션 카테고리 43개 API 전부 확인해도 투자자매매동향 API가 없고(kis-code-assistant-mcp), 키움 레퍼런스 문서·네이버에도 없음(사용자 확인 후 코스피/코스닥 2개 시장으로 축소). `/investor-trend`에 `?market=kospi\|kosdaq` 쿼리파라미터 추가(기본값 kospi, 하위호환 유지). **코스닥 값이 배포 후 몇 개월치가 전부 0으로 나오는 버그 발견·수정**: `fetch_market_investor_daily`가 업종코드(`FID_INPUT_ISCD`/`FID_INPUT_ISCD_2`)를 시장 무관하게 `'0001'`(코스피 종합)로 고정해뒀던 게 원인 - `KSQ`(코스닥) + `0001`(코스피 업종) 조합이 유효하지 않아 200 OK/rt_cd=0인데 금액 필드만 전부 빈 값으로 오는 조용한 실패였음. KRX 업종코드 관례(코스피 0으로 시작/코스닥 1로 시작)를 따라 코스닥은 `'1001'`로 분기해 해결(raw 응답 임시 로깅으로 확인 후 정리함) - `MARKETS` 딕셔너리에 `kis_inds_cd` 필드로 관리. 프론트는 기간 탭(일/주/월) 옆에 시장 탭(코스피/코스닥)을 추가했고, 개인/외국인/기관 3행 분리 레이아웃(`dayRowsHtml`) - 행별 독립 Y스케일, 항상 표시되는 날짜+값 레이블, 행 헤더에 기간합계(▲/▼), 막대 호버 시 `title` 툴팁 - 을 **처음엔 일(日) 탭에만 적용했다가 사용자가 배포 후 확인하고 "주/월도 일 스타일로" 요청해 전체 탭 공용 레이아웃으로 통일**(기존 그룹막대+테이블 코드는 삭제). 색상은 지시서 3-3 원문 그대로(`#888888`/`#e84040`/`#3b82f6`, 음수는 8자리 hex 알파) 적용. 모바일 320px에서 10개 컬럼이 다 안 들어가면 찌그러뜨리는 대신 행 단위로 가로 스크롤(`overflow-x`) - 처음엔 컬럼을 `flex:1 1 0`으로 균등분배했다가 값 레이블이 잘리는 걸 실측으로 발견(30개 컬럼 중 16개 clip), `flex:0 0 auto; min-width` 고정폭 + 스크롤로 수정. |
| `js/market-temp.js`, `css/market-temp.css` | **오늘의 증시온도** - CNN Fear&Greed 스타일 대표 콘텐츠(2026-07-18 전면 개편). VIX20+수급20+거래대금15+평균등락률15+상승비율10+섹터강도10+52주신고저10+환율5+미국선물5=110점을 0~40℃로 환산(GAS `?marketTemp=1`). 섹션 순서: Hero(온도+전일/주간/월간대비+역발상 투자시그널 별점, 공포=매수 신호) → AI 시장 브리핑(`?marketTempBriefing=1`, Groq, TOP5 기여도를 프롬프트에 정확히 명시해 AI가 숫자를 지어내지 않게 함) → 오늘 영향요인 TOP5(점수기여도=score-max/2) → 게이지 → 개별지표(계산식 툴팁, GAS가 내려주는 `band` 필드) → 7일 스파크라인/레이더차트(둘 다 hand-roll SVG, 라이브러리 미사용) → 투자전략 카드 → 기준표 → (기존 유지) 카드보기/히트맵보기/시총비례 탐색. VIX는 Yahoo Finance(`query1.finance.yahoo.com`, 네이버엔 없음), 수급은 KODEX200(069500) 대리지표, 상승비율/거래대금/섹터강도는 섹터 종목풀 재사용, 52주신고저는 VM `/week52-batch`. **애니메이션(count-up/게이지스윕/진행바채움/스파크라인draw)은 rAF·CSS 타임라인이 안 도는 환경(백그라운드 탭 등)에서 값이 고정돼버리는 문제가 실측됐음** - 전부 JS `setTimeout` 안전장치를 병행해 데이터 정확성은 애니메이션 성공 여부와 무관하게 보장됨(향후 이 위젯에 애니메이션 추가 시 같은 패턴 유지할 것) |
| `js/short-pressure.js`, `css/short-pressure.css` | **구 공매도 압박 위젯(네이버/KRX 기반) - 폐기 예정, 코드만 유지.** KRX·네이버 경로 모두 막혀 있었음(무료 소스 없음). `js/foreign-flow.js`의 병합 섹션(키움 API 기반)이 이 기능을 대체함 — 티스토리에 이 위젯을 단독 페이지로 임베드해뒀다면 수동으로 정리 필요(어느 페이지인지 이 저장소에서 확인 불가, skin.html이 gitignore 대상). |
| `js/pension-fund.js`, `css/pension-fund.css` | **구 연기금 위젯(기관 합산 추정치) - 폐기 예정, 코드만 유지.** 연기금 단독 데이터가 없어 기관 합산으로 대체했던 버전. `js/foreign-flow.js`의 병합 섹션이 키움 API의 진짜 연기금(`penfnd_etc`) 데이터로 이 기능을 대체함 — 단독 페이지로 임베드해뒀다면 수동 정리 필요. |
| `test/*.html` | 로컬 프리뷰(python -m http.server로 열기). 위젯마다 기본은 mock 데이터, `?real=1`이면 실제 GAS 호출 |
| `data/sectors-v3-검수표.md` | 종목코드 매핑 검수표 — 섹터 데이터 수정 시 같이 갱신 |
| `js/skin-shell.js` | 스킨의 **태그 없는 순수 UI 조각**(모바일 오버레이·검색 오버레이·스크롤탑 버튼, 드로어 헤더, 서브 필터 바, **사이트 하단 푸터**)을 `skin.html`의 `<div id="shell-*">` mount에 런타임 주입. **반드시 skin-menu.js/skin-main.js보다 먼저 로드**(그 스크립트들이 이 안에서 만든 id를 getElementById로 찾음). 이 파일에 있는 것들은 push만으로 반영, skin.html 재배포 불필요. (구 KRX 공시 티커 껍데기는 2026-07-17(9차)에 제거 — `js/quick-indices.js`의 긴급속보 패널로 흡수됨. `skin.html`의 `#shell-discTicker` mount는 빈 채로 남아있지만 무해함) |
| `legal/*.html`, `css/legal.css` | **사이트 하단 푸터**(서비스 이용약관/개인정보처리방침/오픈소스 라이선스/사이트 사용법, 2026-07-22 신설)의 링크 대상. 티스토리 "페이지"로 만들지 않고 이 저장소의 정적 HTML로 만들어 GitHub Pages로 직접 서빙 — 티스토리 페이지 수동 생성 없이 git push만으로 내용 수정 가능(다른 위젯과 동일 배포 방식). 다른 origin(github.io)이라 다크모드는 `bolt-dark` localStorage 대신 `prefers-color-scheme` 미디어쿼리로 판단. 오픈소스 라이선스 목록(TradingView Lightweight Charts/Pretendard/Google Fonts)은 실제 스킨이 로드하는 CDN 링크 기준으로 작성 — 새 오픈소스 라이브러리를 추가하면 이 목록도 같이 갱신할 것. `guide.html`(사이트 사용법)은 `js/skin-menu.js`의 `MENU_ITEMS` 등 실제 메뉴 구성 기준으로 작성 — 메뉴 추가/삭제 시 같이 갱신할 것. 문의하기 링크는 mailto(페이지 아님), 이 4개 링크는 `js/skin-shell.js`의 `SHELL.footerLinks`에서 관리. **카피라이트(`© [##_blogger_##]`)는 티스토리 서버 치환 태그가 섞여있어 이 링크들과 달리 git으로 못 옮기고 skin.html에 직접 있음** — `<footer class="site-footer">` 래퍼 자체도 skin.html에 있고 `SHELL.footerLinks`는 그 안의 `#shell-footerLinks` 자리만 채움(카피라이트와 같은 행에 배치하려고 래퍼를 공유하는 구조). 카피라이트를 누르면 `guide.html`로 이동. **2026-07-23**: 최종 문구는 `© [##_blogger_##] (사이트 이용 방법)` — 처음엔 "© 2025–2026"(연도 자동갱신 스크립트 포함)이었다가 사용자 요청으로 연도 표기 자체를 삭제(자동갱신 스크립트도 함께 제거). 문의하기와 카피라이트 사이 구분자도 처음엔 별도 텍스트 `<span>\|</span>`였다가, 앞의 링크 구분선(얇은 `border-right` 세로선)과 굵기·색·간격이 안 맞아 보인다는 실측 피드백으로 폐기 — 지금은 `.copyright`에 `border-left`+`padding-left:12px`를 줘서 `.site-footer-links a`의 구분선과 완전히 같은 스타일로 통일함(텍스트 `\|` 글자 없음) |
| `js/skin-menu.js` | 커스텀 메뉴(`#nav-menu-mount`) 렌더링. 메뉴 추가/삭제/순서 변경은 `MENU_ITEMS` 배열만 고치면 됨. **2026-07-27 "9Pay 증권" 개편**: 세로 왼쪽 사이드바를 navbar 바로 아래 가로 상단 메뉴바로 전환("LOGO · 검색 · 아이콘" 헤더 + 그 아래 메뉴 탭 행, 토스증권/네이버페이 증권 참고) - skin.html의 DOM(`.sidebar-left`, `#nav-menu-mount`)은 그대로 두고 style.css로 `position:fixed` 가로바로 재배치만 함(skin.html 재배포 불필요, `--topbar-height` CSS 변수로 높이 관리 - 바꾸면 `.page-wrap`/`.sidebar-right`/quick-indices 오프셋 calc()도 같이 확인). 종목검색 입력창(`SEARCH_HTML`)은 처음엔 이 메뉴바 카드 안에 있었다가 **2026-07-28에 navbar 안(`#navSearchMount`, skin.html)으로 이전** - `render()`가 `#navSearchMount`/`#nav-menu-mount` 두 mount를 독립적으로 채우도록 분리(하나만 있어도 나머지가 죽지 않게). 카테고리 목록·캘린더 카드는 50px로 눌린 가로바에서 넘쳐 보여 skin.html에서 완전히 삭제(사용자 확인, "카테고리 삭제"). **메뉴바 색/정렬(2026-07-28)**: 배경을 navbar와 같은 rgb(253,253,247)(다크모드는 navbar와 같은 #1A1A1A - 원래 #082415 진초록이었다가 같은 날 다크모드 전체 재통일로 교체됨, 기타 규칙 참고)로 통일하고 위아래 회색 hairline으로 구분 - `.nav-menu`가 `.card` 클래스도 갖고 있어 `.card{background:#fff}`가 새어나오는 버그가 있었음(`.sidebar-left .nav-menu`에 `background:transparent` 명시로 해결). 메뉴 탭은 원래 `max-width:1380px;margin:0 auto`로 가운데 정렬이었으나 navbar 로고(왼쪽 끝 고정)와 어긋나 보여 제거하고 navbar와 같은 좌측 padding(14px)으로 맞춤. **2026-07-28**: 메뉴 항목 전체(홈 포함) bold 글자 제거(사용자 요청) - `MENU_ITEMS`의 `it.bold` 플래그(2026-07-18에 "홈만 안 굵어서 통일" 목적으로 전 항목에 붙였던 것)를 아예 제거하고 `render()`의 인라인 `font-weight:700` 분기도 삭제. 활성 탭(현재 페이지) 표시도 `style.css`의 `font-weight:700`을 빼고 색+`border-bottom-color`만으로 구분하도록 변경. |
| `js/stock-search-panel.js` | **navbar 실시간 종목검색**(`#navSearchInput`/`#navSearchSuggest`, 마크업은 skin-menu.js `SEARCH_HTML`, 동작은 이 파일). 입력 없을 때 즐겨찾기(★, `stock:favorites`)+최근검색(`stock:recent`, 최대 10개) 드롭다운, 입력 시 KRX_MAP 자동완성(시작일치 우선, ETF는 일반종목 다음 - `data/krx_map.js`의 `KRX_ETF_NAMES` 활용). 종목 선택 시 `/page/foreign-flow?code=&name=`로 이동(종목분석 페이지 재사용, 새 페이지 안 만듦). **2026-07-28 버그 수정**: 이 검색창의 즐겨찾기(★)가 `js/watchlist.js`의 "MY" 페이지(`wl_codes_v1`)와 완전히 별개 localStorage라 별표를 눌러도 MY에 안 나타나는 문제 발견 - `watchlist.js`는 `/page/watchlist`에만 임베드돼있어 다른 페이지에선 `window.Watchlist`가 없으므로, `isFavorite`/`toggleFavorite`이 `window.Watchlist` 있으면 그 공개 API를 쓰고 없으면 `wl_codes_v1`을 watchlist.js와 동일한 스키마로 직접 read/write하도록 동기화(`isInWatchlist`/`addToWatchlist`/`removeFromWatchlist` 헬퍼). |
| `js/stock-search.js`, `css/stock-search.css` | **증시검색** - 독립 Tistory Page(`/page/stock-search` placeholder, 사용자가 실제 페이지 아직 미생성 - URL 확정되면 `js/skin-menu.js`/`js/watchlist.js`의 `STOCK_SEARCH_PAGE_URL` 상수만 바꾸면 됨). 종목 검색 + `js/order-book.js`의 호가창(`OrderBook.init('#order-book',{hideSearch:true})`으로 재사용, mount id가 리터럴 `order-book`이어야 함 - `css/order-book.css`가 ID로 스코프돼 있어 다른 id로 마운트하면 스타일이 안 먹음) + 가격차트를 한 페이지에 통합. 검색 결과 리스트는 30건까지 나오면 선택 후 자동으로 접혀서("목록 접기/N건·선택:X/다시 보기") 화면을 덜 차지하게 함. 결과 행마다 ⭐ 즐겨찾기 버튼이 `global.Watchlist.add/has/remove`로 바로 MY에 연동(이 페이지엔 처음부터 watchlist.js를 같이 로드해야 즐겨찾기가 동작 - 안 붙어있으면 무반응이니 배포 체크리스트에 포함할 것). `?code=&name=`로 딥링크(watchlist.js의 "차트 보기" 버튼이 여기로 옴). **2026-07-28**: 검색창이 화면 폭 대부분(flex:1)을 차지해 보인다는 피드백으로 1/3 폭(`flex:0 0 33%; min-width:220px`)으로 축소, 좁은 화면(860px 이하)에서는 다시 전체 폭. |
| `js/skin-main.js` | 스킨 공통 동작 — 다크모드/폰트 토글, 카테고리 파싱(좌측 카테고리 목록·서브 필터 탭), 아티클 모달, 모바일 드로어·검색. (구 KRX 공시 티커 fetch/파싱 로직은 2026-07-17(9차)에 `js/quick-indices.js`로 이관됨. **2026-07-22**: 구글 캘린더 위젯 로직(옛 `openCalendarModal`/`initCalendarWidget`, 중앙 모달 방식)을 `js/stock-calendar.js`(독립 페이지)로 옮기며 이 파일에서는 삭제 — 구글 캘린더 API 키 하드코딩·노출 경고도 그쪽으로 함께 이동) |
| `js/stock-calendar.js`, `css/stock-calendar.css` | **증시캘린더** - 독립 Tistory Page(`/page/stock-calendar`, `<div id="stock-calendar">` 임베드)로 운영되는 위젯(2026-07-22 신설, 예전엔 `js/skin-main.js`의 중앙 모달 `openCalendarModal` 방식이었으나 사용자 요청으로 전환). 데이터 소스는 구글 캘린더 이벤트(제목+날짜/시간)뿐 — 예측치/이전치 같은 경제지표 수치는 소스가 없어 표시하지 않음. 이벤트 제목 규칙(사람이 구글 캘린더에 일정 등록할 때 직접 지켜야 함, 자동 감지 아님): `"$종목명 텍스트 | 태그"` — `$종목명`으로 시작하면 종목 이벤트(실적발표 등)로 인식해 아이콘+`$종목명` 뱃지로 표시, 국기 이모지(🇺🇸 등)로 시작하면 해외 지표로 인식해 아이콘에 국기 표시, `\|` 뒤 텍스트는 관심/주요 같은 태그 뱃지로 분리. 월 달력 + 이벤트가 있는 주차만 "N월 M주차" 리스트로 필터 없이 항상 펼쳐서 표시(사용자 요청으로 전체/경제지표/실적·전체/국내/해외 필터 UI는 제거함), 각 행 맨 앞에 `M/D` 날짜 표시(주차 리스트엔 여러 날짜가 섞여 있어 필요, 사용자 요청). **구글 캘린더 API 키와 캘린더 ID가 이 파일에 하드코딩되어 있고 public 저장소로 push됨 — 노출 상태**(키 재발급/리퍼러 제한은 사용자가 Google Cloud Console에서 직접 해야 함). `test/stock-calendar.html`에서 mock 데이터로 로컬 검증 가능(`?real=1`로 실제 구글 캘린더 호출) |

**`skin.html`은 2026-07-27부터 git 추적 대상**(그 전엔 "캘린더 ID 등 개인정보 소지" 우려로 `.gitignore` 제외였으나, 캘린더 위젯이 `js/stock-calendar.js` 독립 페이지로 분리된 뒤라 현재 내용엔 개인정보가 없음을 확인하고 사용자 요청으로 전환함). **단, git에 있다고 해서 push로 라이브에 반영되는 게 아님** — Tistory는 이 저장소를 pull하지 않으므로, 실제 배포는 여전히 티스토리 스킨 편집기(관리자 → 꾸미기 → 스킨 편집 → HTML 편집)에 수동으로 붙여넣어야만 됨. git의 skin.html은 "최신 히스토리/리뷰용 사본"이지 배포 경로가 아니다 — 이 점에서 `style.css`(2026-07-10부터 git이 곧 배포 경로, GitHub Pages가 직접 서빙)와 근본적으로 다르니 혼동하지 말 것.

**skin.html 수정 시 필수 절차** (2026-07-11 사고 이후, 2026-07-27 git 추적 전환 후에도 유효):
1. **git의 skin.html이 실제 라이브 버전과 다를 수 있다는 전제는 여전히 유효.** 사용자가 티스토리 편집기에서 직접 고치고 git에는 반영을 깜빡했을 수 있음 - 중요한 수정 전에는 확신 없으면 사용자에게 티스토리 관리자 HTML 편집 화면의 실제 내용을 다시 확인해달라고 요청할 것.
2. **티스토리 서버 치환 태그(`[##_..._##]`, `<s_xxx>` 블록)가 하나라도 포함된 요소는 절대 git 전용 파일(js/skin-shell.js 등)로 못 옮김** — Tistory가 서버에서 렌더링할 때만 치환되므로 GitHub Pages 정적 파일(JS로 나중에 주입)로는 작동 안 함. 이런 요소가 있는 블록(네비바 로고/검색창, 카테고리 데이터, 방문자 통계, 글·공지 루프, 방명록, 페이지네이션, 인기글, 해시태그, 카피라이트)은 skin.html에 그대로 남겨야 함.
3. **태그가 전혀 없는 블록**만 `js/skin-shell.js`에 옮기고 skin.html에는 빈 `<div id="shell-이름">`만 남긴다 (패턴은 그 파일 참고).
4. skin.html의 순수 UI 구조를 바꿀 때(예: 특정 영역 제거/추가) JS로 런타임에 `.remove()`/`.style.display` 하는 방식은 **깜빡임(FOUC)이 생김** — 이미 로드된 실제 콘텐츠를 나중에 지우는 거라서. 빈 mount + 즉시 채우기 방식(위 패턴)은 깜빡임 없음. 이미 skin.html에 박제된 콘텐츠를 지우고 싶다면 skin.html 자체에서 지워야 함(즉 스킨 편집기 재배포 필요).
5. skin.html을 수정했다면 **반드시 로컬에서 실제로 실행해서 검증**: 티스토리 태그 부분에 목업 데이터(예시 카테고리 목록 등)를 채운 테스트 사본을 만들고, git의 실제 JS 파일들을 로컬 경로로 붙여서 `python -m http.server`로 띄운 뒤 Browser 도구로 콘솔 에러·기능 동작(드로어 열기/닫기, 다크모드, 캘린더 등)을 확인하고 테스트 파일은 삭제한다. "코드가 맞아 보인다"만으로 끝내지 말 것.
6. 검증 끝나면 **git의 skin.html을 커밋·push하고(리뷰/히스토리용), 그 전체 내용을 사용자에게도 다시 안내**해서 티스토리 편집기에 직접 붙여넣게 한다 — git push만으로는 라이브에 반영되지 않으므로 이 수동 붙여넣기 단계는 생략 불가.

## 섹터 데이터 수정 규칙 (가장 잦은 작업)

1. **중복 제거 금지**: 한 종목이 여러 섹터에 의도적으로 중복 포함됨(현대차, 포스코DX, SK이노베이션, 한화에어로스페이스, 풍산, POSCO홀딩스, 삼성물산, GS, LS, LG화학, 두산에너빌리티, 한전기술 등).
2. **종목코드는 추측 금지**: KRX KIND 상장사 목록으로 확인할 것 —
   `https://kind.krx.co.kr/corpgeneral/corpList.do?method=download&marketType=stockMkt`(코스피) / `kosdaqMkt`(코스닥), EUC-KR HTML 테이블. 확인 불가하면 사용자에게 보고.
3. **KIND 등록명과 표기가 다른 종목(별칭 함정)**: 현대차→현대자동차, KT→케이티, KT&G→케이티앤지, KCC→케이씨씨, LS ELECTRIC→엘에스일렉트릭, SK바이오팜→에스케이바이오팜, 삼성화재→삼성화재해상보험, 한국전력→한국전력공사, HDC현대산업개발→IPARK현대산업개발, HD현대건설기계→HD건설기계. 표시명은 블로그 표기를 쓰고 코드만 KIND에서 가져온다.
4. 삼성에피스홀딩스는 특수코드 `0126Z0`(정상). HD현대인프라코어(042670)는 합병 상장폐지라 시세가 안 나옴.
5. 수정 후 검증: `python -m http.server` + `test/sector-dashboard-v4.html` 열어서 해당 섹터 카드에 종목·시세가 뜨는지 확인. 그 다음 commit·push.
6. push 후 반영 확인: `curl https://goodbyestarwars.github.io/tistory-ticker/data/sectors-v3.js` 에 수정 내용이 실렸는지 확인(최대 10분 지연).

## 기타 규칙

- 상승=빨강(#d24f45), 하락=파랑(#1261c4). 스킨 다크모드는 `html.dark` 클래스 — 본문에 들어가는 새 UI는 `html.dark #컨테이너ID ... !important` 오버라이드 필수(스킨의 블랑켓 규칙이 글자색을 흰색으로 덮어씀).
- **2026-07-28: 다크모드 전체를 배경 `#1A1A1A` + 글자 `#F5EFE0` 2색 체계로 재통일**(사용자 요청 - "다크모드인데 navbar가 왜 초록색이냐" 리포트가 계기). `style.css`의 `html.dark` 블록(navbar/상단 메뉴바/카드/본문/푸터/모달/페이지네이션/사이드바 통계 등 사이트 전체 셸)을 전면 재작업 - 예전엔 파일마다 남색(#171e2f)·짙은 남색(#0e1320)·초록(#082415) 등 제각각 톤을 썼던 걸 이 두 색으로 통일했다. **원칙**: 배경에서 "떠 보여야" 하는 카드/모달 등은 별도 색 대신 같은 `#1A1A1A` 위에 `rgba(245,239,224, 낮은 알파)` 테두리/오버레이만 얹어 2색 체계를 지킨다(예: 카드 테두리 `rgba(245,239,224,0.12)`, hover 배경 `rgba(245,239,224,0.08)`). **단, "의미"를 담은 색은 그대로 유지**(전부 지우면 정보가 사라짐) — 상승/하락 빨강·파랑, 카테고리 뱃지 색(글 분류 구분용), 사이드바 통계 아이콘 4색(si-blue/amber/purple/green), 즐겨찾기 별 금색, 온도계 빨강, 본문 인라인 색상 보존 규칙(사용자가 직접 칠한 빨강/파랑/초록/주황/보라) 등은 예외로 남겨뒀다. h2/h3/굵은글씨는 원래 본문과 다른 색(흰색/보라 계열)으로 위계를 줬었는데 2색 체계에선 색 대신 기존 폰트 크기/굵기 차이로만 위계를 구분하도록 바꿈. **아직 안 한 것**: 이 작업은 `style.css`(사이트 공통 셸)까지만 진행했고, `css/quick-indices.js`·`css/overnight-market.css`·`css/market-temp.css`·`css/marketcap-bubble.css` 등 개별 위젯 파일(`html.dark` 규칙이 있는 파일은 총 20여 개)은 각자의 다크 팔레트를 그대로 쓰고 있음 - 사용자가 이어서 요청하면 위와 같은 원칙(구조색=2색 통일, 의미색=보존)으로 위젯별로 마저 정리할 것.
- 시세 조회 GAS URL: `https://script.google.com/macros/s/AKfycbzhKxOqOzw6N1xjW0Jhj5tlbiN0PMRdrQQD6nORBTlP0NDAOvtKfidHU2xwMAbV33mOuQ/exec?codes=005930,000660` 형태.
- `.json` 업로드 제약 때문에 데이터는 전부 `window.XXX = {...}` 형태의 `.js`로 만든다.
- **KRX 내부 크롤링 경로(`data.krx.co.kr/comm/bldAttendant/getJsonData.cmd`, OTP+CSV 다운로드 경로 `generate.cmd` 포함)는 2026-07-11부로 완전 차단.** 세션 쿠키를 붙여도, OTP 발급을 시도해도 "LOGOUT"으로 거부됨(실측 확인됨). KRX 정식 Open API(`openapi.krx.co.kr`)에도 공매도·투자자별 매매동향 서비스 자체가 없음 — **새 KRX 직접 크롤링 코드를 다시 추가하지 말 것.**
- **공매도/대차거래/연기금은 KRX 대신 증권사(키움) 정식 REST API로 우회한다.** `scripts/fetch_investor_flow.py` 참고. 이 데이터는 사용자 개인 계좌의 API 키가 필요해 GAS 같은 공개 서버에 둘 수 없고, 앱키가 IP 등록 방식이라 GitHub Actions 같은 유동 IP 클라우드도 못 씀 — 그래서 PC 로컬 실행 + git push 구조. 오픈API 이용약관(재배포/제3자 제공 제한 여부)은 미확인 상태로 진행 중임을 인지할 것.
